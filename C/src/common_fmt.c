// Generated enums and structures for Mavlink dialect common #0 version 3
// Generated by gomavlink, DO NOT EDIT.
#include <stdarg.h>
#include <stdio.h>

#include "common.h"

static inline size_t psnprintf(char **pbuf, size_t *psize, const char *fmt,
                               ...) {
  va_list ap;
  va_start(ap, fmt);
  size_t s = vsnprintf(*pbuf, *psize, fmt, ap);
  if (s < *psize) {
    *pbuf += s;
    *psize -= s;
  } else {
    *psize = 0;
  }
  return s;
}

static const char *MAV_AUTOPILOT_str(enum MAV_AUTOPILOT val) {
  switch (val) {
  case MAV_AUTOPILOT_GENERIC:
    return "MAV_AUTOPILOT_GENERIC";
  case MAV_AUTOPILOT_RESERVED:
    return "MAV_AUTOPILOT_RESERVED";
  case MAV_AUTOPILOT_SLUGS:
    return "MAV_AUTOPILOT_SLUGS";
  case MAV_AUTOPILOT_ARDUPILOTMEGA:
    return "MAV_AUTOPILOT_ARDUPILOTMEGA";
  case MAV_AUTOPILOT_OPENPILOT:
    return "MAV_AUTOPILOT_OPENPILOT";
  case MAV_AUTOPILOT_GENERIC_WAYPOINTS_ONLY:
    return "MAV_AUTOPILOT_GENERIC_WAYPOINTS_ONLY";
  case MAV_AUTOPILOT_GENERIC_WAYPOINTS_AND_SIMPLE_NAVIGATION_ONLY:
    return "MAV_AUTOPILOT_GENERIC_WAYPOINTS_AND_SIMPLE_NAVIGATION_ONLY";
  case MAV_AUTOPILOT_GENERIC_MISSION_FULL:
    return "MAV_AUTOPILOT_GENERIC_MISSION_FULL";
  case MAV_AUTOPILOT_INVALID:
    return "MAV_AUTOPILOT_INVALID";
  case MAV_AUTOPILOT_PPZ:
    return "MAV_AUTOPILOT_PPZ";
  case MAV_AUTOPILOT_UDB:
    return "MAV_AUTOPILOT_UDB";
  case MAV_AUTOPILOT_FP:
    return "MAV_AUTOPILOT_FP";
  case MAV_AUTOPILOT_PX4:
    return "MAV_AUTOPILOT_PX4";
  case MAV_AUTOPILOT_SMACCMPILOT:
    return "MAV_AUTOPILOT_SMACCMPILOT";
  case MAV_AUTOPILOT_AUTOQUAD:
    return "MAV_AUTOPILOT_AUTOQUAD";
  case MAV_AUTOPILOT_ARMAZILA:
    return "MAV_AUTOPILOT_ARMAZILA";
  case MAV_AUTOPILOT_AEROB:
    return "MAV_AUTOPILOT_AEROB";
  case MAV_AUTOPILOT_ASLUAV:
    return "MAV_AUTOPILOT_ASLUAV";
  case MAV_AUTOPILOT_SMARTAP:
    return "MAV_AUTOPILOT_SMARTAP";
  case MAV_AUTOPILOT_AIRRAILS:
    return "MAV_AUTOPILOT_AIRRAILS";
  }
  return NULL;
};

static size_t psnprint_MAV_AUTOPILOT(char **p, size_t *sz,
                                     enum MAV_AUTOPILOT val) {
  const char *s = MAV_AUTOPILOT_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_AUTOPILOT<%d>", (int)val);
}

static const char *MAV_TYPE_str(enum MAV_TYPE val) {
  switch (val) {
  case MAV_TYPE_GENERIC:
    return "MAV_TYPE_GENERIC";
  case MAV_TYPE_FIXED_WING:
    return "MAV_TYPE_FIXED_WING";
  case MAV_TYPE_QUADROTOR:
    return "MAV_TYPE_QUADROTOR";
  case MAV_TYPE_COAXIAL:
    return "MAV_TYPE_COAXIAL";
  case MAV_TYPE_HELICOPTER:
    return "MAV_TYPE_HELICOPTER";
  case MAV_TYPE_ANTENNA_TRACKER:
    return "MAV_TYPE_ANTENNA_TRACKER";
  case MAV_TYPE_GCS:
    return "MAV_TYPE_GCS";
  case MAV_TYPE_AIRSHIP:
    return "MAV_TYPE_AIRSHIP";
  case MAV_TYPE_FREE_BALLOON:
    return "MAV_TYPE_FREE_BALLOON";
  case MAV_TYPE_ROCKET:
    return "MAV_TYPE_ROCKET";
  case MAV_TYPE_GROUND_ROVER:
    return "MAV_TYPE_GROUND_ROVER";
  case MAV_TYPE_SURFACE_BOAT:
    return "MAV_TYPE_SURFACE_BOAT";
  case MAV_TYPE_SUBMARINE:
    return "MAV_TYPE_SUBMARINE";
  case MAV_TYPE_HEXAROTOR:
    return "MAV_TYPE_HEXAROTOR";
  case MAV_TYPE_OCTOROTOR:
    return "MAV_TYPE_OCTOROTOR";
  case MAV_TYPE_TRICOPTER:
    return "MAV_TYPE_TRICOPTER";
  case MAV_TYPE_FLAPPING_WING:
    return "MAV_TYPE_FLAPPING_WING";
  case MAV_TYPE_KITE:
    return "MAV_TYPE_KITE";
  case MAV_TYPE_ONBOARD_CONTROLLER:
    return "MAV_TYPE_ONBOARD_CONTROLLER";
  case MAV_TYPE_VTOL_DUOROTOR:
    return "MAV_TYPE_VTOL_DUOROTOR";
  case MAV_TYPE_VTOL_QUADROTOR:
    return "MAV_TYPE_VTOL_QUADROTOR";
  case MAV_TYPE_VTOL_TILTROTOR:
    return "MAV_TYPE_VTOL_TILTROTOR";
  case MAV_TYPE_VTOL_RESERVED2:
    return "MAV_TYPE_VTOL_RESERVED2";
  case MAV_TYPE_VTOL_RESERVED3:
    return "MAV_TYPE_VTOL_RESERVED3";
  case MAV_TYPE_VTOL_RESERVED4:
    return "MAV_TYPE_VTOL_RESERVED4";
  case MAV_TYPE_VTOL_RESERVED5:
    return "MAV_TYPE_VTOL_RESERVED5";
  case MAV_TYPE_GIMBAL:
    return "MAV_TYPE_GIMBAL";
  case MAV_TYPE_ADSB:
    return "MAV_TYPE_ADSB";
  case MAV_TYPE_PARAFOIL:
    return "MAV_TYPE_PARAFOIL";
  case MAV_TYPE_DODECAROTOR:
    return "MAV_TYPE_DODECAROTOR";
  case MAV_TYPE_CAMERA:
    return "MAV_TYPE_CAMERA";
  case MAV_TYPE_CHARGING_STATION:
    return "MAV_TYPE_CHARGING_STATION";
  case MAV_TYPE_FLARM:
    return "MAV_TYPE_FLARM";
  }
  return NULL;
};

static size_t psnprint_MAV_TYPE(char **p, size_t *sz, enum MAV_TYPE val) {
  const char *s = MAV_TYPE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_TYPE<%d>", (int)val);
}

static const char *FIRMWARE_VERSION_TYPE_str(enum FIRMWARE_VERSION_TYPE val) {
  switch (val) {
  case FIRMWARE_VERSION_TYPE_DEV:
    return "FIRMWARE_VERSION_TYPE_DEV";
  case FIRMWARE_VERSION_TYPE_ALPHA:
    return "FIRMWARE_VERSION_TYPE_ALPHA";
  case FIRMWARE_VERSION_TYPE_BETA:
    return "FIRMWARE_VERSION_TYPE_BETA";
  case FIRMWARE_VERSION_TYPE_RC:
    return "FIRMWARE_VERSION_TYPE_RC";
  case FIRMWARE_VERSION_TYPE_OFFICIAL:
    return "FIRMWARE_VERSION_TYPE_OFFICIAL";
  }
  return NULL;
};

static size_t psnprint_FIRMWARE_VERSION_TYPE(char **p, size_t *sz,
                                             enum FIRMWARE_VERSION_TYPE val) {
  const char *s = FIRMWARE_VERSION_TYPE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "FIRMWARE_VERSION_TYPE<%d>", (int)val);
}

static const char *HL_FAILURE_FLAG_str(enum HL_FAILURE_FLAG val) {
  switch (val) {
  case HL_FAILURE_FLAG_GPS:
    return "HL_FAILURE_FLAG_GPS";
  case HL_FAILURE_FLAG_DIFFERENTIAL_PRESSURE:
    return "HL_FAILURE_FLAG_DIFFERENTIAL_PRESSURE";
  case HL_FAILURE_FLAG_ABSOLUTE_PRESSURE:
    return "HL_FAILURE_FLAG_ABSOLUTE_PRESSURE";
  case HL_FAILURE_FLAG_3D_ACCEL:
    return "HL_FAILURE_FLAG_3D_ACCEL";
  case HL_FAILURE_FLAG_3D_GYRO:
    return "HL_FAILURE_FLAG_3D_GYRO";
  case HL_FAILURE_FLAG_3D_MAG:
    return "HL_FAILURE_FLAG_3D_MAG";
  case HL_FAILURE_FLAG_TERRAIN:
    return "HL_FAILURE_FLAG_TERRAIN";
  case HL_FAILURE_FLAG_BATTERY:
    return "HL_FAILURE_FLAG_BATTERY";
  case HL_FAILURE_FLAG_RC_RECEIVER:
    return "HL_FAILURE_FLAG_RC_RECEIVER";
  case HL_FAILURE_FLAG_OFFBOARD_LINK:
    return "HL_FAILURE_FLAG_OFFBOARD_LINK";
  case HL_FAILURE_FLAG_ENGINE:
    return "HL_FAILURE_FLAG_ENGINE";
  case HL_FAILURE_FLAG_GEOFENCE:
    return "HL_FAILURE_FLAG_GEOFENCE";
  case HL_FAILURE_FLAG_ESTIMATOR:
    return "HL_FAILURE_FLAG_ESTIMATOR";
  case HL_FAILURE_FLAG_MISSION:
    return "HL_FAILURE_FLAG_MISSION";
  }
  return NULL;
};

static size_t psnprint_HL_FAILURE_FLAG(char **p, size_t *sz,
                                       enum HL_FAILURE_FLAG val) {
  const char *s = HL_FAILURE_FLAG_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "HL_FAILURE_FLAG<%d>", (int)val);
}

static const char *MAV_MODE_FLAG_str(enum MAV_MODE_FLAG val) {
  switch (val) {
  case MAV_MODE_FLAG_SAFETY_ARMED:
    return "MAV_MODE_FLAG_SAFETY_ARMED";
  case MAV_MODE_FLAG_MANUAL_INPUT_ENABLED:
    return "MAV_MODE_FLAG_MANUAL_INPUT_ENABLED";
  case MAV_MODE_FLAG_HIL_ENABLED:
    return "MAV_MODE_FLAG_HIL_ENABLED";
  case MAV_MODE_FLAG_STABILIZE_ENABLED:
    return "MAV_MODE_FLAG_STABILIZE_ENABLED";
  case MAV_MODE_FLAG_GUIDED_ENABLED:
    return "MAV_MODE_FLAG_GUIDED_ENABLED";
  case MAV_MODE_FLAG_AUTO_ENABLED:
    return "MAV_MODE_FLAG_AUTO_ENABLED";
  case MAV_MODE_FLAG_TEST_ENABLED:
    return "MAV_MODE_FLAG_TEST_ENABLED";
  case MAV_MODE_FLAG_CUSTOM_MODE_ENABLED:
    return "MAV_MODE_FLAG_CUSTOM_MODE_ENABLED";
  }
  return NULL;
};

static size_t psnprint_MAV_MODE_FLAG(char **p, size_t *sz,
                                     enum MAV_MODE_FLAG val) {
  const char *s = MAV_MODE_FLAG_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_MODE_FLAG<%d>", (int)val);
}

static const char *
MAV_MODE_FLAG_DECODE_POSITION_str(enum MAV_MODE_FLAG_DECODE_POSITION val) {
  switch (val) {
  case MAV_MODE_FLAG_DECODE_POSITION_SAFETY:
    return "MAV_MODE_FLAG_DECODE_POSITION_SAFETY";
  case MAV_MODE_FLAG_DECODE_POSITION_MANUAL:
    return "MAV_MODE_FLAG_DECODE_POSITION_MANUAL";
  case MAV_MODE_FLAG_DECODE_POSITION_HIL:
    return "MAV_MODE_FLAG_DECODE_POSITION_HIL";
  case MAV_MODE_FLAG_DECODE_POSITION_STABILIZE:
    return "MAV_MODE_FLAG_DECODE_POSITION_STABILIZE";
  case MAV_MODE_FLAG_DECODE_POSITION_GUIDED:
    return "MAV_MODE_FLAG_DECODE_POSITION_GUIDED";
  case MAV_MODE_FLAG_DECODE_POSITION_AUTO:
    return "MAV_MODE_FLAG_DECODE_POSITION_AUTO";
  case MAV_MODE_FLAG_DECODE_POSITION_TEST:
    return "MAV_MODE_FLAG_DECODE_POSITION_TEST";
  case MAV_MODE_FLAG_DECODE_POSITION_CUSTOM_MODE:
    return "MAV_MODE_FLAG_DECODE_POSITION_CUSTOM_MODE";
  }
  return NULL;
};

static size_t
psnprint_MAV_MODE_FLAG_DECODE_POSITION(char **p, size_t *sz,
                                       enum MAV_MODE_FLAG_DECODE_POSITION val) {
  const char *s = MAV_MODE_FLAG_DECODE_POSITION_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_MODE_FLAG_DECODE_POSITION<%d>", (int)val);
}

static const char *MAV_GOTO_str(enum MAV_GOTO val) {
  switch (val) {
  case MAV_GOTO_DO_HOLD:
    return "MAV_GOTO_DO_HOLD";
  case MAV_GOTO_DO_CONTINUE:
    return "MAV_GOTO_DO_CONTINUE";
  case MAV_GOTO_HOLD_AT_CURRENT_POSITION:
    return "MAV_GOTO_HOLD_AT_CURRENT_POSITION";
  case MAV_GOTO_HOLD_AT_SPECIFIED_POSITION:
    return "MAV_GOTO_HOLD_AT_SPECIFIED_POSITION";
  }
  return NULL;
};

static size_t psnprint_MAV_GOTO(char **p, size_t *sz, enum MAV_GOTO val) {
  const char *s = MAV_GOTO_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_GOTO<%d>", (int)val);
}

static const char *MAV_MODE_str(enum MAV_MODE val) {
  switch (val) {
  case MAV_MODE_PREFLIGHT:
    return "MAV_MODE_PREFLIGHT";
  case MAV_MODE_STABILIZE_DISARMED:
    return "MAV_MODE_STABILIZE_DISARMED";
  case MAV_MODE_STABILIZE_ARMED:
    return "MAV_MODE_STABILIZE_ARMED";
  case MAV_MODE_MANUAL_DISARMED:
    return "MAV_MODE_MANUAL_DISARMED";
  case MAV_MODE_MANUAL_ARMED:
    return "MAV_MODE_MANUAL_ARMED";
  case MAV_MODE_GUIDED_DISARMED:
    return "MAV_MODE_GUIDED_DISARMED";
  case MAV_MODE_GUIDED_ARMED:
    return "MAV_MODE_GUIDED_ARMED";
  case MAV_MODE_AUTO_DISARMED:
    return "MAV_MODE_AUTO_DISARMED";
  case MAV_MODE_AUTO_ARMED:
    return "MAV_MODE_AUTO_ARMED";
  case MAV_MODE_TEST_DISARMED:
    return "MAV_MODE_TEST_DISARMED";
  case MAV_MODE_TEST_ARMED:
    return "MAV_MODE_TEST_ARMED";
  }
  return NULL;
};

static size_t psnprint_MAV_MODE(char **p, size_t *sz, enum MAV_MODE val) {
  const char *s = MAV_MODE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_MODE<%d>", (int)val);
}

static const char *MAV_STATE_str(enum MAV_STATE val) {
  switch (val) {
  case MAV_STATE_UNINIT:
    return "MAV_STATE_UNINIT";
  case MAV_STATE_BOOT:
    return "MAV_STATE_BOOT";
  case MAV_STATE_CALIBRATING:
    return "MAV_STATE_CALIBRATING";
  case MAV_STATE_STANDBY:
    return "MAV_STATE_STANDBY";
  case MAV_STATE_ACTIVE:
    return "MAV_STATE_ACTIVE";
  case MAV_STATE_CRITICAL:
    return "MAV_STATE_CRITICAL";
  case MAV_STATE_EMERGENCY:
    return "MAV_STATE_EMERGENCY";
  case MAV_STATE_POWEROFF:
    return "MAV_STATE_POWEROFF";
  case MAV_STATE_FLIGHT_TERMINATION:
    return "MAV_STATE_FLIGHT_TERMINATION";
  }
  return NULL;
};

static size_t psnprint_MAV_STATE(char **p, size_t *sz, enum MAV_STATE val) {
  const char *s = MAV_STATE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_STATE<%d>", (int)val);
}

static const char *MAV_COMPONENT_str(enum MAV_COMPONENT val) {
  switch (val) {
  case MAV_COMP_ID_ALL:
    return "MAV_COMP_ID_ALL";
  case MAV_COMP_ID_AUTOPILOT1:
    return "MAV_COMP_ID_AUTOPILOT1";
  case MAV_COMP_ID_CAMERA:
    return "MAV_COMP_ID_CAMERA";
  case MAV_COMP_ID_CAMERA2:
    return "MAV_COMP_ID_CAMERA2";
  case MAV_COMP_ID_CAMERA3:
    return "MAV_COMP_ID_CAMERA3";
  case MAV_COMP_ID_CAMERA4:
    return "MAV_COMP_ID_CAMERA4";
  case MAV_COMP_ID_CAMERA5:
    return "MAV_COMP_ID_CAMERA5";
  case MAV_COMP_ID_CAMERA6:
    return "MAV_COMP_ID_CAMERA6";
  case MAV_COMP_ID_SERVO1:
    return "MAV_COMP_ID_SERVO1";
  case MAV_COMP_ID_SERVO2:
    return "MAV_COMP_ID_SERVO2";
  case MAV_COMP_ID_SERVO3:
    return "MAV_COMP_ID_SERVO3";
  case MAV_COMP_ID_SERVO4:
    return "MAV_COMP_ID_SERVO4";
  case MAV_COMP_ID_SERVO5:
    return "MAV_COMP_ID_SERVO5";
  case MAV_COMP_ID_SERVO6:
    return "MAV_COMP_ID_SERVO6";
  case MAV_COMP_ID_SERVO7:
    return "MAV_COMP_ID_SERVO7";
  case MAV_COMP_ID_SERVO8:
    return "MAV_COMP_ID_SERVO8";
  case MAV_COMP_ID_SERVO9:
    return "MAV_COMP_ID_SERVO9";
  case MAV_COMP_ID_SERVO10:
    return "MAV_COMP_ID_SERVO10";
  case MAV_COMP_ID_SERVO11:
    return "MAV_COMP_ID_SERVO11";
  case MAV_COMP_ID_SERVO12:
    return "MAV_COMP_ID_SERVO12";
  case MAV_COMP_ID_SERVO13:
    return "MAV_COMP_ID_SERVO13";
  case MAV_COMP_ID_SERVO14:
    return "MAV_COMP_ID_SERVO14";
  case MAV_COMP_ID_GIMBAL:
    return "MAV_COMP_ID_GIMBAL";
  case MAV_COMP_ID_LOG:
    return "MAV_COMP_ID_LOG";
  case MAV_COMP_ID_ADSB:
    return "MAV_COMP_ID_ADSB";
  case MAV_COMP_ID_OSD:
    return "MAV_COMP_ID_OSD";
  case MAV_COMP_ID_PERIPHERAL:
    return "MAV_COMP_ID_PERIPHERAL";
  case MAV_COMP_ID_QX1_GIMBAL:
    return "MAV_COMP_ID_QX1_GIMBAL";
  case MAV_COMP_ID_FLARM:
    return "MAV_COMP_ID_FLARM";
  case MAV_COMP_ID_MISSIONPLANNER:
    return "MAV_COMP_ID_MISSIONPLANNER";
  case MAV_COMP_ID_PATHPLANNER:
    return "MAV_COMP_ID_PATHPLANNER";
  case MAV_COMP_ID_OBSTACLE_AVOIDANCE:
    return "MAV_COMP_ID_OBSTACLE_AVOIDANCE";
  case MAV_COMP_ID_VISUAL_INERTIAL_ODOMETRY:
    return "MAV_COMP_ID_VISUAL_INERTIAL_ODOMETRY";
  case MAV_COMP_ID_IMU:
    return "MAV_COMP_ID_IMU";
  case MAV_COMP_ID_IMU_2:
    return "MAV_COMP_ID_IMU_2";
  case MAV_COMP_ID_IMU_3:
    return "MAV_COMP_ID_IMU_3";
  case MAV_COMP_ID_GPS:
    return "MAV_COMP_ID_GPS";
  case MAV_COMP_ID_GPS2:
    return "MAV_COMP_ID_GPS2";
  case MAV_COMP_ID_UDP_BRIDGE:
    return "MAV_COMP_ID_UDP_BRIDGE";
  case MAV_COMP_ID_UART_BRIDGE:
    return "MAV_COMP_ID_UART_BRIDGE";
  case MAV_COMP_ID_SYSTEM_CONTROL:
    return "MAV_COMP_ID_SYSTEM_CONTROL";
  }
  return NULL;
};

static size_t psnprint_MAV_COMPONENT(char **p, size_t *sz,
                                     enum MAV_COMPONENT val) {
  const char *s = MAV_COMPONENT_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_COMPONENT<%d>", (int)val);
}

static const char *MAV_SYS_STATUS_SENSOR_str(enum MAV_SYS_STATUS_SENSOR val) {
  switch (val) {
  case MAV_SYS_STATUS_SENSOR_3D_GYRO:
    return "MAV_SYS_STATUS_SENSOR_3D_GYRO";
  case MAV_SYS_STATUS_SENSOR_3D_ACCEL:
    return "MAV_SYS_STATUS_SENSOR_3D_ACCEL";
  case MAV_SYS_STATUS_SENSOR_3D_MAG:
    return "MAV_SYS_STATUS_SENSOR_3D_MAG";
  case MAV_SYS_STATUS_SENSOR_ABSOLUTE_PRESSURE:
    return "MAV_SYS_STATUS_SENSOR_ABSOLUTE_PRESSURE";
  case MAV_SYS_STATUS_SENSOR_DIFFERENTIAL_PRESSURE:
    return "MAV_SYS_STATUS_SENSOR_DIFFERENTIAL_PRESSURE";
  case MAV_SYS_STATUS_SENSOR_GPS:
    return "MAV_SYS_STATUS_SENSOR_GPS";
  case MAV_SYS_STATUS_SENSOR_OPTICAL_FLOW:
    return "MAV_SYS_STATUS_SENSOR_OPTICAL_FLOW";
  case MAV_SYS_STATUS_SENSOR_VISION_POSITION:
    return "MAV_SYS_STATUS_SENSOR_VISION_POSITION";
  case MAV_SYS_STATUS_SENSOR_LASER_POSITION:
    return "MAV_SYS_STATUS_SENSOR_LASER_POSITION";
  case MAV_SYS_STATUS_SENSOR_EXTERNAL_GROUND_TRUTH:
    return "MAV_SYS_STATUS_SENSOR_EXTERNAL_GROUND_TRUTH";
  case MAV_SYS_STATUS_SENSOR_ANGULAR_RATE_CONTROL:
    return "MAV_SYS_STATUS_SENSOR_ANGULAR_RATE_CONTROL";
  case MAV_SYS_STATUS_SENSOR_ATTITUDE_STABILIZATION:
    return "MAV_SYS_STATUS_SENSOR_ATTITUDE_STABILIZATION";
  case MAV_SYS_STATUS_SENSOR_YAW_POSITION:
    return "MAV_SYS_STATUS_SENSOR_YAW_POSITION";
  case MAV_SYS_STATUS_SENSOR_Z_ALTITUDE_CONTROL:
    return "MAV_SYS_STATUS_SENSOR_Z_ALTITUDE_CONTROL";
  case MAV_SYS_STATUS_SENSOR_XY_POSITION_CONTROL:
    return "MAV_SYS_STATUS_SENSOR_XY_POSITION_CONTROL";
  case MAV_SYS_STATUS_SENSOR_MOTOR_OUTPUTS:
    return "MAV_SYS_STATUS_SENSOR_MOTOR_OUTPUTS";
  case MAV_SYS_STATUS_SENSOR_RC_RECEIVER:
    return "MAV_SYS_STATUS_SENSOR_RC_RECEIVER";
  case MAV_SYS_STATUS_SENSOR_3D_GYRO2:
    return "MAV_SYS_STATUS_SENSOR_3D_GYRO2";
  case MAV_SYS_STATUS_SENSOR_3D_ACCEL2:
    return "MAV_SYS_STATUS_SENSOR_3D_ACCEL2";
  case MAV_SYS_STATUS_SENSOR_3D_MAG2:
    return "MAV_SYS_STATUS_SENSOR_3D_MAG2";
  case MAV_SYS_STATUS_GEOFENCE:
    return "MAV_SYS_STATUS_GEOFENCE";
  case MAV_SYS_STATUS_AHRS:
    return "MAV_SYS_STATUS_AHRS";
  case MAV_SYS_STATUS_TERRAIN:
    return "MAV_SYS_STATUS_TERRAIN";
  case MAV_SYS_STATUS_REVERSE_MOTOR:
    return "MAV_SYS_STATUS_REVERSE_MOTOR";
  case MAV_SYS_STATUS_LOGGING:
    return "MAV_SYS_STATUS_LOGGING";
  case MAV_SYS_STATUS_SENSOR_BATTERY:
    return "MAV_SYS_STATUS_SENSOR_BATTERY";
  case MAV_SYS_STATUS_SENSOR_PROXIMITY:
    return "MAV_SYS_STATUS_SENSOR_PROXIMITY";
  case MAV_SYS_STATUS_SENSOR_SATCOM:
    return "MAV_SYS_STATUS_SENSOR_SATCOM";
  }
  return NULL;
};

static size_t psnprint_MAV_SYS_STATUS_SENSOR(char **p, size_t *sz,
                                             enum MAV_SYS_STATUS_SENSOR val) {
  const char *s = MAV_SYS_STATUS_SENSOR_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_SYS_STATUS_SENSOR<%d>", (int)val);
}

static const char *MAV_FRAME_str(enum MAV_FRAME val) {
  switch (val) {
  case MAV_FRAME_GLOBAL:
    return "MAV_FRAME_GLOBAL";
  case MAV_FRAME_LOCAL_NED:
    return "MAV_FRAME_LOCAL_NED";
  case MAV_FRAME_MISSION:
    return "MAV_FRAME_MISSION";
  case MAV_FRAME_GLOBAL_RELATIVE_ALT:
    return "MAV_FRAME_GLOBAL_RELATIVE_ALT";
  case MAV_FRAME_LOCAL_ENU:
    return "MAV_FRAME_LOCAL_ENU";
  case MAV_FRAME_GLOBAL_INT:
    return "MAV_FRAME_GLOBAL_INT";
  case MAV_FRAME_GLOBAL_RELATIVE_ALT_INT:
    return "MAV_FRAME_GLOBAL_RELATIVE_ALT_INT";
  case MAV_FRAME_LOCAL_OFFSET_NED:
    return "MAV_FRAME_LOCAL_OFFSET_NED";
  case MAV_FRAME_BODY_NED:
    return "MAV_FRAME_BODY_NED";
  case MAV_FRAME_BODY_OFFSET_NED:
    return "MAV_FRAME_BODY_OFFSET_NED";
  case MAV_FRAME_GLOBAL_TERRAIN_ALT:
    return "MAV_FRAME_GLOBAL_TERRAIN_ALT";
  case MAV_FRAME_GLOBAL_TERRAIN_ALT_INT:
    return "MAV_FRAME_GLOBAL_TERRAIN_ALT_INT";
  case MAV_FRAME_BODY_FRD:
    return "MAV_FRAME_BODY_FRD";
  case MAV_FRAME_BODY_FLU:
    return "MAV_FRAME_BODY_FLU";
  case MAV_FRAME_MOCAP_NED:
    return "MAV_FRAME_MOCAP_NED";
  case MAV_FRAME_MOCAP_ENU:
    return "MAV_FRAME_MOCAP_ENU";
  case MAV_FRAME_VISION_NED:
    return "MAV_FRAME_VISION_NED";
  case MAV_FRAME_VISION_ENU:
    return "MAV_FRAME_VISION_ENU";
  case MAV_FRAME_ESTIM_NED:
    return "MAV_FRAME_ESTIM_NED";
  case MAV_FRAME_ESTIM_ENU:
    return "MAV_FRAME_ESTIM_ENU";
  }
  return NULL;
};

static size_t psnprint_MAV_FRAME(char **p, size_t *sz, enum MAV_FRAME val) {
  const char *s = MAV_FRAME_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_FRAME<%d>", (int)val);
}

static const char *
MAVLINK_DATA_STREAM_TYPE_str(enum MAVLINK_DATA_STREAM_TYPE val) {
  switch (val) {
  case MAVLINK_DATA_STREAM_IMG_JPEG:
    return "MAVLINK_DATA_STREAM_IMG_JPEG";
  case MAVLINK_DATA_STREAM_IMG_BMP:
    return "MAVLINK_DATA_STREAM_IMG_BMP";
  case MAVLINK_DATA_STREAM_IMG_RAW8U:
    return "MAVLINK_DATA_STREAM_IMG_RAW8U";
  case MAVLINK_DATA_STREAM_IMG_RAW32U:
    return "MAVLINK_DATA_STREAM_IMG_RAW32U";
  case MAVLINK_DATA_STREAM_IMG_PGM:
    return "MAVLINK_DATA_STREAM_IMG_PGM";
  case MAVLINK_DATA_STREAM_IMG_PNG:
    return "MAVLINK_DATA_STREAM_IMG_PNG";
  }
  return NULL;
};

static size_t
psnprint_MAVLINK_DATA_STREAM_TYPE(char **p, size_t *sz,
                                  enum MAVLINK_DATA_STREAM_TYPE val) {
  const char *s = MAVLINK_DATA_STREAM_TYPE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAVLINK_DATA_STREAM_TYPE<%d>", (int)val);
}

static const char *FENCE_ACTION_str(enum FENCE_ACTION val) {
  switch (val) {
  case FENCE_ACTION_NONE:
    return "FENCE_ACTION_NONE";
  case FENCE_ACTION_GUIDED:
    return "FENCE_ACTION_GUIDED";
  case FENCE_ACTION_REPORT:
    return "FENCE_ACTION_REPORT";
  case FENCE_ACTION_GUIDED_THR_PASS:
    return "FENCE_ACTION_GUIDED_THR_PASS";
  case FENCE_ACTION_RTL:
    return "FENCE_ACTION_RTL";
  }
  return NULL;
};

static size_t psnprint_FENCE_ACTION(char **p, size_t *sz,
                                    enum FENCE_ACTION val) {
  const char *s = FENCE_ACTION_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "FENCE_ACTION<%d>", (int)val);
}

static const char *FENCE_BREACH_str(enum FENCE_BREACH val) {
  switch (val) {
  case FENCE_BREACH_NONE:
    return "FENCE_BREACH_NONE";
  case FENCE_BREACH_MINALT:
    return "FENCE_BREACH_MINALT";
  case FENCE_BREACH_MAXALT:
    return "FENCE_BREACH_MAXALT";
  case FENCE_BREACH_BOUNDARY:
    return "FENCE_BREACH_BOUNDARY";
  }
  return NULL;
};

static size_t psnprint_FENCE_BREACH(char **p, size_t *sz,
                                    enum FENCE_BREACH val) {
  const char *s = FENCE_BREACH_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "FENCE_BREACH<%d>", (int)val);
}

static const char *MAV_MOUNT_MODE_str(enum MAV_MOUNT_MODE val) {
  switch (val) {
  case MAV_MOUNT_MODE_RETRACT:
    return "MAV_MOUNT_MODE_RETRACT";
  case MAV_MOUNT_MODE_NEUTRAL:
    return "MAV_MOUNT_MODE_NEUTRAL";
  case MAV_MOUNT_MODE_MAVLINK_TARGETING:
    return "MAV_MOUNT_MODE_MAVLINK_TARGETING";
  case MAV_MOUNT_MODE_RC_TARGETING:
    return "MAV_MOUNT_MODE_RC_TARGETING";
  case MAV_MOUNT_MODE_GPS_POINT:
    return "MAV_MOUNT_MODE_GPS_POINT";
  }
  return NULL;
};

static size_t psnprint_MAV_MOUNT_MODE(char **p, size_t *sz,
                                      enum MAV_MOUNT_MODE val) {
  const char *s = MAV_MOUNT_MODE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_MOUNT_MODE<%d>", (int)val);
}

static const char *UAVCAN_NODE_HEALTH_str(enum UAVCAN_NODE_HEALTH val) {
  switch (val) {
  case UAVCAN_NODE_HEALTH_OK:
    return "UAVCAN_NODE_HEALTH_OK";
  case UAVCAN_NODE_HEALTH_WARNING:
    return "UAVCAN_NODE_HEALTH_WARNING";
  case UAVCAN_NODE_HEALTH_ERROR:
    return "UAVCAN_NODE_HEALTH_ERROR";
  case UAVCAN_NODE_HEALTH_CRITICAL:
    return "UAVCAN_NODE_HEALTH_CRITICAL";
  }
  return NULL;
};

static size_t psnprint_UAVCAN_NODE_HEALTH(char **p, size_t *sz,
                                          enum UAVCAN_NODE_HEALTH val) {
  const char *s = UAVCAN_NODE_HEALTH_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "UAVCAN_NODE_HEALTH<%d>", (int)val);
}

static const char *UAVCAN_NODE_MODE_str(enum UAVCAN_NODE_MODE val) {
  switch (val) {
  case UAVCAN_NODE_MODE_OPERATIONAL:
    return "UAVCAN_NODE_MODE_OPERATIONAL";
  case UAVCAN_NODE_MODE_INITIALIZATION:
    return "UAVCAN_NODE_MODE_INITIALIZATION";
  case UAVCAN_NODE_MODE_MAINTENANCE:
    return "UAVCAN_NODE_MODE_MAINTENANCE";
  case UAVCAN_NODE_MODE_SOFTWARE_UPDATE:
    return "UAVCAN_NODE_MODE_SOFTWARE_UPDATE";
  case UAVCAN_NODE_MODE_OFFLINE:
    return "UAVCAN_NODE_MODE_OFFLINE";
  }
  return NULL;
};

static size_t psnprint_UAVCAN_NODE_MODE(char **p, size_t *sz,
                                        enum UAVCAN_NODE_MODE val) {
  const char *s = UAVCAN_NODE_MODE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "UAVCAN_NODE_MODE<%d>", (int)val);
}

static const char *STORAGE_STATUS_str(enum STORAGE_STATUS val) {
  switch (val) {
  case STORAGE_STATUS_EMPTY:
    return "STORAGE_STATUS_EMPTY";
  case STORAGE_STATUS_UNFORMATTED:
    return "STORAGE_STATUS_UNFORMATTED";
  case STORAGE_STATUS_READY:
    return "STORAGE_STATUS_READY";
  case STORAGE_STATUS_NOT_SUPPORTED:
    return "STORAGE_STATUS_NOT_SUPPORTED";
  }
  return NULL;
};

static size_t psnprint_STORAGE_STATUS(char **p, size_t *sz,
                                      enum STORAGE_STATUS val) {
  const char *s = STORAGE_STATUS_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "STORAGE_STATUS<%d>", (int)val);
}

static const char *MAV_CMD_str(enum MAV_CMD val) {
  switch (val) {
  case MAV_CMD_NAV_WAYPOINT:
    return "MAV_CMD_NAV_WAYPOINT";
  case MAV_CMD_NAV_LOITER_UNLIM:
    return "MAV_CMD_NAV_LOITER_UNLIM";
  case MAV_CMD_NAV_LOITER_TURNS:
    return "MAV_CMD_NAV_LOITER_TURNS";
  case MAV_CMD_NAV_LOITER_TIME:
    return "MAV_CMD_NAV_LOITER_TIME";
  case MAV_CMD_NAV_RETURN_TO_LAUNCH:
    return "MAV_CMD_NAV_RETURN_TO_LAUNCH";
  case MAV_CMD_NAV_LAND:
    return "MAV_CMD_NAV_LAND";
  case MAV_CMD_NAV_TAKEOFF:
    return "MAV_CMD_NAV_TAKEOFF";
  case MAV_CMD_NAV_LAND_LOCAL:
    return "MAV_CMD_NAV_LAND_LOCAL";
  case MAV_CMD_NAV_TAKEOFF_LOCAL:
    return "MAV_CMD_NAV_TAKEOFF_LOCAL";
  case MAV_CMD_NAV_FOLLOW:
    return "MAV_CMD_NAV_FOLLOW";
  case MAV_CMD_NAV_CONTINUE_AND_CHANGE_ALT:
    return "MAV_CMD_NAV_CONTINUE_AND_CHANGE_ALT";
  case MAV_CMD_NAV_LOITER_TO_ALT:
    return "MAV_CMD_NAV_LOITER_TO_ALT";
  case MAV_CMD_DO_FOLLOW:
    return "MAV_CMD_DO_FOLLOW";
  case MAV_CMD_DO_FOLLOW_REPOSITION:
    return "MAV_CMD_DO_FOLLOW_REPOSITION";
  case MAV_CMD_DO_ORBIT:
    return "MAV_CMD_DO_ORBIT";
  case MAV_CMD_NAV_ROI:
    return "MAV_CMD_NAV_ROI";
  case MAV_CMD_NAV_PATHPLANNING:
    return "MAV_CMD_NAV_PATHPLANNING";
  case MAV_CMD_NAV_SPLINE_WAYPOINT:
    return "MAV_CMD_NAV_SPLINE_WAYPOINT";
  case MAV_CMD_NAV_VTOL_TAKEOFF:
    return "MAV_CMD_NAV_VTOL_TAKEOFF";
  case MAV_CMD_NAV_VTOL_LAND:
    return "MAV_CMD_NAV_VTOL_LAND";
  case MAV_CMD_NAV_GUIDED_ENABLE:
    return "MAV_CMD_NAV_GUIDED_ENABLE";
  case MAV_CMD_NAV_DELAY:
    return "MAV_CMD_NAV_DELAY";
  case MAV_CMD_NAV_PAYLOAD_PLACE:
    return "MAV_CMD_NAV_PAYLOAD_PLACE";
  case MAV_CMD_NAV_LAST:
    return "MAV_CMD_NAV_LAST";
  case MAV_CMD_CONDITION_DELAY:
    return "MAV_CMD_CONDITION_DELAY";
  case MAV_CMD_CONDITION_CHANGE_ALT:
    return "MAV_CMD_CONDITION_CHANGE_ALT";
  case MAV_CMD_CONDITION_DISTANCE:
    return "MAV_CMD_CONDITION_DISTANCE";
  case MAV_CMD_CONDITION_YAW:
    return "MAV_CMD_CONDITION_YAW";
  case MAV_CMD_CONDITION_LAST:
    return "MAV_CMD_CONDITION_LAST";
  case MAV_CMD_DO_SET_MODE:
    return "MAV_CMD_DO_SET_MODE";
  case MAV_CMD_DO_JUMP:
    return "MAV_CMD_DO_JUMP";
  case MAV_CMD_DO_CHANGE_SPEED:
    return "MAV_CMD_DO_CHANGE_SPEED";
  case MAV_CMD_DO_SET_HOME:
    return "MAV_CMD_DO_SET_HOME";
  case MAV_CMD_DO_SET_PARAMETER:
    return "MAV_CMD_DO_SET_PARAMETER";
  case MAV_CMD_DO_SET_RELAY:
    return "MAV_CMD_DO_SET_RELAY";
  case MAV_CMD_DO_REPEAT_RELAY:
    return "MAV_CMD_DO_REPEAT_RELAY";
  case MAV_CMD_DO_SET_SERVO:
    return "MAV_CMD_DO_SET_SERVO";
  case MAV_CMD_DO_REPEAT_SERVO:
    return "MAV_CMD_DO_REPEAT_SERVO";
  case MAV_CMD_DO_FLIGHTTERMINATION:
    return "MAV_CMD_DO_FLIGHTTERMINATION";
  case MAV_CMD_DO_CHANGE_ALTITUDE:
    return "MAV_CMD_DO_CHANGE_ALTITUDE";
  case MAV_CMD_DO_LAND_START:
    return "MAV_CMD_DO_LAND_START";
  case MAV_CMD_DO_RALLY_LAND:
    return "MAV_CMD_DO_RALLY_LAND";
  case MAV_CMD_DO_GO_AROUND:
    return "MAV_CMD_DO_GO_AROUND";
  case MAV_CMD_DO_REPOSITION:
    return "MAV_CMD_DO_REPOSITION";
  case MAV_CMD_DO_PAUSE_CONTINUE:
    return "MAV_CMD_DO_PAUSE_CONTINUE";
  case MAV_CMD_DO_SET_REVERSE:
    return "MAV_CMD_DO_SET_REVERSE";
  case MAV_CMD_DO_SET_ROI_LOCATION:
    return "MAV_CMD_DO_SET_ROI_LOCATION";
  case MAV_CMD_DO_SET_ROI_WPNEXT_OFFSET:
    return "MAV_CMD_DO_SET_ROI_WPNEXT_OFFSET";
  case MAV_CMD_DO_SET_ROI_NONE:
    return "MAV_CMD_DO_SET_ROI_NONE";
  case MAV_CMD_DO_CONTROL_VIDEO:
    return "MAV_CMD_DO_CONTROL_VIDEO";
  case MAV_CMD_DO_SET_ROI:
    return "MAV_CMD_DO_SET_ROI";
  case MAV_CMD_DO_DIGICAM_CONFIGURE:
    return "MAV_CMD_DO_DIGICAM_CONFIGURE";
  case MAV_CMD_DO_DIGICAM_CONTROL:
    return "MAV_CMD_DO_DIGICAM_CONTROL";
  case MAV_CMD_DO_MOUNT_CONFIGURE:
    return "MAV_CMD_DO_MOUNT_CONFIGURE";
  case MAV_CMD_DO_MOUNT_CONTROL:
    return "MAV_CMD_DO_MOUNT_CONTROL";
  case MAV_CMD_DO_SET_CAM_TRIGG_DIST:
    return "MAV_CMD_DO_SET_CAM_TRIGG_DIST";
  case MAV_CMD_DO_FENCE_ENABLE:
    return "MAV_CMD_DO_FENCE_ENABLE";
  case MAV_CMD_DO_PARACHUTE:
    return "MAV_CMD_DO_PARACHUTE";
  case MAV_CMD_DO_MOTOR_TEST:
    return "MAV_CMD_DO_MOTOR_TEST";
  case MAV_CMD_DO_INVERTED_FLIGHT:
    return "MAV_CMD_DO_INVERTED_FLIGHT";
  case MAV_CMD_NAV_SET_YAW_SPEED:
    return "MAV_CMD_NAV_SET_YAW_SPEED";
  case MAV_CMD_DO_SET_CAM_TRIGG_INTERVAL:
    return "MAV_CMD_DO_SET_CAM_TRIGG_INTERVAL";
  case MAV_CMD_DO_MOUNT_CONTROL_QUAT:
    return "MAV_CMD_DO_MOUNT_CONTROL_QUAT";
  case MAV_CMD_DO_GUIDED_MASTER:
    return "MAV_CMD_DO_GUIDED_MASTER";
  case MAV_CMD_DO_GUIDED_LIMITS:
    return "MAV_CMD_DO_GUIDED_LIMITS";
  case MAV_CMD_DO_ENGINE_CONTROL:
    return "MAV_CMD_DO_ENGINE_CONTROL";
  case MAV_CMD_DO_SET_MISSION_CURRENT:
    return "MAV_CMD_DO_SET_MISSION_CURRENT";
  case MAV_CMD_DO_LAST:
    return "MAV_CMD_DO_LAST";
  case MAV_CMD_PREFLIGHT_CALIBRATION:
    return "MAV_CMD_PREFLIGHT_CALIBRATION";
  case MAV_CMD_PREFLIGHT_SET_SENSOR_OFFSETS:
    return "MAV_CMD_PREFLIGHT_SET_SENSOR_OFFSETS";
  case MAV_CMD_PREFLIGHT_UAVCAN:
    return "MAV_CMD_PREFLIGHT_UAVCAN";
  case MAV_CMD_PREFLIGHT_STORAGE:
    return "MAV_CMD_PREFLIGHT_STORAGE";
  case MAV_CMD_PREFLIGHT_REBOOT_SHUTDOWN:
    return "MAV_CMD_PREFLIGHT_REBOOT_SHUTDOWN";
  case MAV_CMD_OVERRIDE_GOTO:
    return "MAV_CMD_OVERRIDE_GOTO";
  case MAV_CMD_MISSION_START:
    return "MAV_CMD_MISSION_START";
  case MAV_CMD_COMPONENT_ARM_DISARM:
    return "MAV_CMD_COMPONENT_ARM_DISARM";
  case MAV_CMD_GET_HOME_POSITION:
    return "MAV_CMD_GET_HOME_POSITION";
  case MAV_CMD_START_RX_PAIR:
    return "MAV_CMD_START_RX_PAIR";
  case MAV_CMD_GET_MESSAGE_INTERVAL:
    return "MAV_CMD_GET_MESSAGE_INTERVAL";
  case MAV_CMD_SET_MESSAGE_INTERVAL:
    return "MAV_CMD_SET_MESSAGE_INTERVAL";
  case MAV_CMD_REQUEST_MESSAGE:
    return "MAV_CMD_REQUEST_MESSAGE";
  case MAV_CMD_REQUEST_PROTOCOL_VERSION:
    return "MAV_CMD_REQUEST_PROTOCOL_VERSION";
  case MAV_CMD_REQUEST_AUTOPILOT_CAPABILITIES:
    return "MAV_CMD_REQUEST_AUTOPILOT_CAPABILITIES";
  case MAV_CMD_REQUEST_CAMERA_INFORMATION:
    return "MAV_CMD_REQUEST_CAMERA_INFORMATION";
  case MAV_CMD_REQUEST_CAMERA_SETTINGS:
    return "MAV_CMD_REQUEST_CAMERA_SETTINGS";
  case MAV_CMD_REQUEST_STORAGE_INFORMATION:
    return "MAV_CMD_REQUEST_STORAGE_INFORMATION";
  case MAV_CMD_STORAGE_FORMAT:
    return "MAV_CMD_STORAGE_FORMAT";
  case MAV_CMD_REQUEST_CAMERA_CAPTURE_STATUS:
    return "MAV_CMD_REQUEST_CAMERA_CAPTURE_STATUS";
  case MAV_CMD_REQUEST_FLIGHT_INFORMATION:
    return "MAV_CMD_REQUEST_FLIGHT_INFORMATION";
  case MAV_CMD_RESET_CAMERA_SETTINGS:
    return "MAV_CMD_RESET_CAMERA_SETTINGS";
  case MAV_CMD_SET_CAMERA_MODE:
    return "MAV_CMD_SET_CAMERA_MODE";
  case MAV_CMD_SET_CAMERA_ZOOM:
    return "MAV_CMD_SET_CAMERA_ZOOM";
  case MAV_CMD_SET_CAMERA_FOCUS:
    return "MAV_CMD_SET_CAMERA_FOCUS";
  case MAV_CMD_JUMP_TAG:
    return "MAV_CMD_JUMP_TAG";
  case MAV_CMD_DO_JUMP_TAG:
    return "MAV_CMD_DO_JUMP_TAG";
  case MAV_CMD_IMAGE_START_CAPTURE:
    return "MAV_CMD_IMAGE_START_CAPTURE";
  case MAV_CMD_IMAGE_STOP_CAPTURE:
    return "MAV_CMD_IMAGE_STOP_CAPTURE";
  case MAV_CMD_REQUEST_CAMERA_IMAGE_CAPTURE:
    return "MAV_CMD_REQUEST_CAMERA_IMAGE_CAPTURE";
  case MAV_CMD_DO_TRIGGER_CONTROL:
    return "MAV_CMD_DO_TRIGGER_CONTROL";
  case MAV_CMD_VIDEO_START_CAPTURE:
    return "MAV_CMD_VIDEO_START_CAPTURE";
  case MAV_CMD_VIDEO_STOP_CAPTURE:
    return "MAV_CMD_VIDEO_STOP_CAPTURE";
  case MAV_CMD_VIDEO_START_STREAMING:
    return "MAV_CMD_VIDEO_START_STREAMING";
  case MAV_CMD_VIDEO_STOP_STREAMING:
    return "MAV_CMD_VIDEO_STOP_STREAMING";
  case MAV_CMD_REQUEST_VIDEO_STREAM_INFORMATION:
    return "MAV_CMD_REQUEST_VIDEO_STREAM_INFORMATION";
  case MAV_CMD_REQUEST_VIDEO_STREAM_STATUS:
    return "MAV_CMD_REQUEST_VIDEO_STREAM_STATUS";
  case MAV_CMD_LOGGING_START:
    return "MAV_CMD_LOGGING_START";
  case MAV_CMD_LOGGING_STOP:
    return "MAV_CMD_LOGGING_STOP";
  case MAV_CMD_AIRFRAME_CONFIGURATION:
    return "MAV_CMD_AIRFRAME_CONFIGURATION";
  case MAV_CMD_CONTROL_HIGH_LATENCY:
    return "MAV_CMD_CONTROL_HIGH_LATENCY";
  case MAV_CMD_PANORAMA_CREATE:
    return "MAV_CMD_PANORAMA_CREATE";
  case MAV_CMD_DO_VTOL_TRANSITION:
    return "MAV_CMD_DO_VTOL_TRANSITION";
  case MAV_CMD_ARM_AUTHORIZATION_REQUEST:
    return "MAV_CMD_ARM_AUTHORIZATION_REQUEST";
  case MAV_CMD_SET_GUIDED_SUBMODE_STANDARD:
    return "MAV_CMD_SET_GUIDED_SUBMODE_STANDARD";
  case MAV_CMD_SET_GUIDED_SUBMODE_CIRCLE:
    return "MAV_CMD_SET_GUIDED_SUBMODE_CIRCLE";
  case MAV_CMD_CONDITION_GATE:
    return "MAV_CMD_CONDITION_GATE";
  case MAV_CMD_NAV_FENCE_RETURN_POINT:
    return "MAV_CMD_NAV_FENCE_RETURN_POINT";
  case MAV_CMD_NAV_FENCE_POLYGON_VERTEX_INCLUSION:
    return "MAV_CMD_NAV_FENCE_POLYGON_VERTEX_INCLUSION";
  case MAV_CMD_NAV_FENCE_POLYGON_VERTEX_EXCLUSION:
    return "MAV_CMD_NAV_FENCE_POLYGON_VERTEX_EXCLUSION";
  case MAV_CMD_NAV_FENCE_CIRCLE_INCLUSION:
    return "MAV_CMD_NAV_FENCE_CIRCLE_INCLUSION";
  case MAV_CMD_NAV_FENCE_CIRCLE_EXCLUSION:
    return "MAV_CMD_NAV_FENCE_CIRCLE_EXCLUSION";
  case MAV_CMD_NAV_RALLY_POINT:
    return "MAV_CMD_NAV_RALLY_POINT";
  case MAV_CMD_UAVCAN_GET_NODE_INFO:
    return "MAV_CMD_UAVCAN_GET_NODE_INFO";
  case MAV_CMD_PAYLOAD_PREPARE_DEPLOY:
    return "MAV_CMD_PAYLOAD_PREPARE_DEPLOY";
  case MAV_CMD_PAYLOAD_CONTROL_DEPLOY:
    return "MAV_CMD_PAYLOAD_CONTROL_DEPLOY";
  case MAV_CMD_WAYPOINT_USER_1:
    return "MAV_CMD_WAYPOINT_USER_1";
  case MAV_CMD_WAYPOINT_USER_2:
    return "MAV_CMD_WAYPOINT_USER_2";
  case MAV_CMD_WAYPOINT_USER_3:
    return "MAV_CMD_WAYPOINT_USER_3";
  case MAV_CMD_WAYPOINT_USER_4:
    return "MAV_CMD_WAYPOINT_USER_4";
  case MAV_CMD_WAYPOINT_USER_5:
    return "MAV_CMD_WAYPOINT_USER_5";
  case MAV_CMD_SPATIAL_USER_1:
    return "MAV_CMD_SPATIAL_USER_1";
  case MAV_CMD_SPATIAL_USER_2:
    return "MAV_CMD_SPATIAL_USER_2";
  case MAV_CMD_SPATIAL_USER_3:
    return "MAV_CMD_SPATIAL_USER_3";
  case MAV_CMD_SPATIAL_USER_4:
    return "MAV_CMD_SPATIAL_USER_4";
  case MAV_CMD_SPATIAL_USER_5:
    return "MAV_CMD_SPATIAL_USER_5";
  case MAV_CMD_USER_1:
    return "MAV_CMD_USER_1";
  case MAV_CMD_USER_2:
    return "MAV_CMD_USER_2";
  case MAV_CMD_USER_3:
    return "MAV_CMD_USER_3";
  case MAV_CMD_USER_4:
    return "MAV_CMD_USER_4";
  case MAV_CMD_USER_5:
    return "MAV_CMD_USER_5";
  }
  return NULL;
};

static size_t psnprint_MAV_CMD(char **p, size_t *sz, enum MAV_CMD val) {
  const char *s = MAV_CMD_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_CMD<%d>", (int)val);
}

static const char *MAV_DATA_STREAM_str(enum MAV_DATA_STREAM val) {
  switch (val) {
  case MAV_DATA_STREAM_ALL:
    return "MAV_DATA_STREAM_ALL";
  case MAV_DATA_STREAM_RAW_SENSORS:
    return "MAV_DATA_STREAM_RAW_SENSORS";
  case MAV_DATA_STREAM_EXTENDED_STATUS:
    return "MAV_DATA_STREAM_EXTENDED_STATUS";
  case MAV_DATA_STREAM_RC_CHANNELS:
    return "MAV_DATA_STREAM_RC_CHANNELS";
  case MAV_DATA_STREAM_RAW_CONTROLLER:
    return "MAV_DATA_STREAM_RAW_CONTROLLER";
  case MAV_DATA_STREAM_POSITION:
    return "MAV_DATA_STREAM_POSITION";
  case MAV_DATA_STREAM_EXTRA1:
    return "MAV_DATA_STREAM_EXTRA1";
  case MAV_DATA_STREAM_EXTRA2:
    return "MAV_DATA_STREAM_EXTRA2";
  case MAV_DATA_STREAM_EXTRA3:
    return "MAV_DATA_STREAM_EXTRA3";
  }
  return NULL;
};

static size_t psnprint_MAV_DATA_STREAM(char **p, size_t *sz,
                                       enum MAV_DATA_STREAM val) {
  const char *s = MAV_DATA_STREAM_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_DATA_STREAM<%d>", (int)val);
}

static const char *MAV_ROI_str(enum MAV_ROI val) {
  switch (val) {
  case MAV_ROI_NONE:
    return "MAV_ROI_NONE";
  case MAV_ROI_WPNEXT:
    return "MAV_ROI_WPNEXT";
  case MAV_ROI_WPINDEX:
    return "MAV_ROI_WPINDEX";
  case MAV_ROI_LOCATION:
    return "MAV_ROI_LOCATION";
  case MAV_ROI_TARGET:
    return "MAV_ROI_TARGET";
  }
  return NULL;
};

static size_t psnprint_MAV_ROI(char **p, size_t *sz, enum MAV_ROI val) {
  const char *s = MAV_ROI_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_ROI<%d>", (int)val);
}

static const char *MAV_CMD_ACK_str(enum MAV_CMD_ACK val) {
  switch (val) {
  case MAV_CMD_ACK_OK:
    return "MAV_CMD_ACK_OK";
  case MAV_CMD_ACK_ERR_FAIL:
    return "MAV_CMD_ACK_ERR_FAIL";
  case MAV_CMD_ACK_ERR_ACCESS_DENIED:
    return "MAV_CMD_ACK_ERR_ACCESS_DENIED";
  case MAV_CMD_ACK_ERR_NOT_SUPPORTED:
    return "MAV_CMD_ACK_ERR_NOT_SUPPORTED";
  case MAV_CMD_ACK_ERR_COORDINATE_FRAME_NOT_SUPPORTED:
    return "MAV_CMD_ACK_ERR_COORDINATE_FRAME_NOT_SUPPORTED";
  case MAV_CMD_ACK_ERR_COORDINATES_OUT_OF_RANGE:
    return "MAV_CMD_ACK_ERR_COORDINATES_OUT_OF_RANGE";
  case MAV_CMD_ACK_ERR_X_LAT_OUT_OF_RANGE:
    return "MAV_CMD_ACK_ERR_X_LAT_OUT_OF_RANGE";
  case MAV_CMD_ACK_ERR_Y_LON_OUT_OF_RANGE:
    return "MAV_CMD_ACK_ERR_Y_LON_OUT_OF_RANGE";
  case MAV_CMD_ACK_ERR_Z_ALT_OUT_OF_RANGE:
    return "MAV_CMD_ACK_ERR_Z_ALT_OUT_OF_RANGE";
  }
  return NULL;
};

static size_t psnprint_MAV_CMD_ACK(char **p, size_t *sz, enum MAV_CMD_ACK val) {
  const char *s = MAV_CMD_ACK_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_CMD_ACK<%d>", (int)val);
}

static const char *MAV_PARAM_TYPE_str(enum MAV_PARAM_TYPE val) {
  switch (val) {
  case MAV_PARAM_TYPE_UINT8:
    return "MAV_PARAM_TYPE_UINT8";
  case MAV_PARAM_TYPE_INT8:
    return "MAV_PARAM_TYPE_INT8";
  case MAV_PARAM_TYPE_UINT16:
    return "MAV_PARAM_TYPE_UINT16";
  case MAV_PARAM_TYPE_INT16:
    return "MAV_PARAM_TYPE_INT16";
  case MAV_PARAM_TYPE_UINT32:
    return "MAV_PARAM_TYPE_UINT32";
  case MAV_PARAM_TYPE_INT32:
    return "MAV_PARAM_TYPE_INT32";
  case MAV_PARAM_TYPE_UINT64:
    return "MAV_PARAM_TYPE_UINT64";
  case MAV_PARAM_TYPE_INT64:
    return "MAV_PARAM_TYPE_INT64";
  case MAV_PARAM_TYPE_REAL32:
    return "MAV_PARAM_TYPE_REAL32";
  case MAV_PARAM_TYPE_REAL64:
    return "MAV_PARAM_TYPE_REAL64";
  }
  return NULL;
};

static size_t psnprint_MAV_PARAM_TYPE(char **p, size_t *sz,
                                      enum MAV_PARAM_TYPE val) {
  const char *s = MAV_PARAM_TYPE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_PARAM_TYPE<%d>", (int)val);
}

static const char *MAV_PARAM_EXT_TYPE_str(enum MAV_PARAM_EXT_TYPE val) {
  switch (val) {
  case MAV_PARAM_EXT_TYPE_UINT8:
    return "MAV_PARAM_EXT_TYPE_UINT8";
  case MAV_PARAM_EXT_TYPE_INT8:
    return "MAV_PARAM_EXT_TYPE_INT8";
  case MAV_PARAM_EXT_TYPE_UINT16:
    return "MAV_PARAM_EXT_TYPE_UINT16";
  case MAV_PARAM_EXT_TYPE_INT16:
    return "MAV_PARAM_EXT_TYPE_INT16";
  case MAV_PARAM_EXT_TYPE_UINT32:
    return "MAV_PARAM_EXT_TYPE_UINT32";
  case MAV_PARAM_EXT_TYPE_INT32:
    return "MAV_PARAM_EXT_TYPE_INT32";
  case MAV_PARAM_EXT_TYPE_UINT64:
    return "MAV_PARAM_EXT_TYPE_UINT64";
  case MAV_PARAM_EXT_TYPE_INT64:
    return "MAV_PARAM_EXT_TYPE_INT64";
  case MAV_PARAM_EXT_TYPE_REAL32:
    return "MAV_PARAM_EXT_TYPE_REAL32";
  case MAV_PARAM_EXT_TYPE_REAL64:
    return "MAV_PARAM_EXT_TYPE_REAL64";
  case MAV_PARAM_EXT_TYPE_CUSTOM:
    return "MAV_PARAM_EXT_TYPE_CUSTOM";
  }
  return NULL;
};

static size_t psnprint_MAV_PARAM_EXT_TYPE(char **p, size_t *sz,
                                          enum MAV_PARAM_EXT_TYPE val) {
  const char *s = MAV_PARAM_EXT_TYPE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_PARAM_EXT_TYPE<%d>", (int)val);
}

static const char *MAV_RESULT_str(enum MAV_RESULT val) {
  switch (val) {
  case MAV_RESULT_ACCEPTED:
    return "MAV_RESULT_ACCEPTED";
  case MAV_RESULT_TEMPORARILY_REJECTED:
    return "MAV_RESULT_TEMPORARILY_REJECTED";
  case MAV_RESULT_DENIED:
    return "MAV_RESULT_DENIED";
  case MAV_RESULT_UNSUPPORTED:
    return "MAV_RESULT_UNSUPPORTED";
  case MAV_RESULT_FAILED:
    return "MAV_RESULT_FAILED";
  case MAV_RESULT_IN_PROGRESS:
    return "MAV_RESULT_IN_PROGRESS";
  }
  return NULL;
};

static size_t psnprint_MAV_RESULT(char **p, size_t *sz, enum MAV_RESULT val) {
  const char *s = MAV_RESULT_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_RESULT<%d>", (int)val);
}

static const char *MAV_MISSION_RESULT_str(enum MAV_MISSION_RESULT val) {
  switch (val) {
  case MAV_MISSION_ACCEPTED:
    return "MAV_MISSION_ACCEPTED";
  case MAV_MISSION_ERROR:
    return "MAV_MISSION_ERROR";
  case MAV_MISSION_UNSUPPORTED_FRAME:
    return "MAV_MISSION_UNSUPPORTED_FRAME";
  case MAV_MISSION_UNSUPPORTED:
    return "MAV_MISSION_UNSUPPORTED";
  case MAV_MISSION_NO_SPACE:
    return "MAV_MISSION_NO_SPACE";
  case MAV_MISSION_INVALID:
    return "MAV_MISSION_INVALID";
  case MAV_MISSION_INVALID_PARAM1:
    return "MAV_MISSION_INVALID_PARAM1";
  case MAV_MISSION_INVALID_PARAM2:
    return "MAV_MISSION_INVALID_PARAM2";
  case MAV_MISSION_INVALID_PARAM3:
    return "MAV_MISSION_INVALID_PARAM3";
  case MAV_MISSION_INVALID_PARAM4:
    return "MAV_MISSION_INVALID_PARAM4";
  case MAV_MISSION_INVALID_PARAM5_X:
    return "MAV_MISSION_INVALID_PARAM5_X";
  case MAV_MISSION_INVALID_PARAM6_Y:
    return "MAV_MISSION_INVALID_PARAM6_Y";
  case MAV_MISSION_INVALID_PARAM7:
    return "MAV_MISSION_INVALID_PARAM7";
  case MAV_MISSION_INVALID_SEQUENCE:
    return "MAV_MISSION_INVALID_SEQUENCE";
  case MAV_MISSION_DENIED:
    return "MAV_MISSION_DENIED";
  case MAV_MISSION_OPERATION_CANCELLED:
    return "MAV_MISSION_OPERATION_CANCELLED";
  }
  return NULL;
};

static size_t psnprint_MAV_MISSION_RESULT(char **p, size_t *sz,
                                          enum MAV_MISSION_RESULT val) {
  const char *s = MAV_MISSION_RESULT_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_MISSION_RESULT<%d>", (int)val);
}

static const char *MAV_SEVERITY_str(enum MAV_SEVERITY val) {
  switch (val) {
  case MAV_SEVERITY_EMERGENCY:
    return "MAV_SEVERITY_EMERGENCY";
  case MAV_SEVERITY_ALERT:
    return "MAV_SEVERITY_ALERT";
  case MAV_SEVERITY_CRITICAL:
    return "MAV_SEVERITY_CRITICAL";
  case MAV_SEVERITY_ERROR:
    return "MAV_SEVERITY_ERROR";
  case MAV_SEVERITY_WARNING:
    return "MAV_SEVERITY_WARNING";
  case MAV_SEVERITY_NOTICE:
    return "MAV_SEVERITY_NOTICE";
  case MAV_SEVERITY_INFO:
    return "MAV_SEVERITY_INFO";
  case MAV_SEVERITY_DEBUG:
    return "MAV_SEVERITY_DEBUG";
  }
  return NULL;
};

static size_t psnprint_MAV_SEVERITY(char **p, size_t *sz,
                                    enum MAV_SEVERITY val) {
  const char *s = MAV_SEVERITY_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_SEVERITY<%d>", (int)val);
}

static const char *MAV_POWER_STATUS_str(enum MAV_POWER_STATUS val) {
  switch (val) {
  case MAV_POWER_STATUS_BRICK_VALID:
    return "MAV_POWER_STATUS_BRICK_VALID";
  case MAV_POWER_STATUS_SERVO_VALID:
    return "MAV_POWER_STATUS_SERVO_VALID";
  case MAV_POWER_STATUS_USB_CONNECTED:
    return "MAV_POWER_STATUS_USB_CONNECTED";
  case MAV_POWER_STATUS_PERIPH_OVERCURRENT:
    return "MAV_POWER_STATUS_PERIPH_OVERCURRENT";
  case MAV_POWER_STATUS_PERIPH_HIPOWER_OVERCURRENT:
    return "MAV_POWER_STATUS_PERIPH_HIPOWER_OVERCURRENT";
  case MAV_POWER_STATUS_CHANGED:
    return "MAV_POWER_STATUS_CHANGED";
  }
  return NULL;
};

static size_t psnprint_MAV_POWER_STATUS(char **p, size_t *sz,
                                        enum MAV_POWER_STATUS val) {
  const char *s = MAV_POWER_STATUS_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_POWER_STATUS<%d>", (int)val);
}

static const char *SERIAL_CONTROL_DEV_str(enum SERIAL_CONTROL_DEV val) {
  switch (val) {
  case SERIAL_CONTROL_DEV_TELEM1:
    return "SERIAL_CONTROL_DEV_TELEM1";
  case SERIAL_CONTROL_DEV_TELEM2:
    return "SERIAL_CONTROL_DEV_TELEM2";
  case SERIAL_CONTROL_DEV_GPS1:
    return "SERIAL_CONTROL_DEV_GPS1";
  case SERIAL_CONTROL_DEV_GPS2:
    return "SERIAL_CONTROL_DEV_GPS2";
  case SERIAL_CONTROL_DEV_SHELL:
    return "SERIAL_CONTROL_DEV_SHELL";
  }
  return NULL;
};

static size_t psnprint_SERIAL_CONTROL_DEV(char **p, size_t *sz,
                                          enum SERIAL_CONTROL_DEV val) {
  const char *s = SERIAL_CONTROL_DEV_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "SERIAL_CONTROL_DEV<%d>", (int)val);
}

static const char *SERIAL_CONTROL_FLAG_str(enum SERIAL_CONTROL_FLAG val) {
  switch (val) {
  case SERIAL_CONTROL_FLAG_REPLY:
    return "SERIAL_CONTROL_FLAG_REPLY";
  case SERIAL_CONTROL_FLAG_RESPOND:
    return "SERIAL_CONTROL_FLAG_RESPOND";
  case SERIAL_CONTROL_FLAG_EXCLUSIVE:
    return "SERIAL_CONTROL_FLAG_EXCLUSIVE";
  case SERIAL_CONTROL_FLAG_BLOCKING:
    return "SERIAL_CONTROL_FLAG_BLOCKING";
  case SERIAL_CONTROL_FLAG_MULTI:
    return "SERIAL_CONTROL_FLAG_MULTI";
  }
  return NULL;
};

static size_t psnprint_SERIAL_CONTROL_FLAG(char **p, size_t *sz,
                                           enum SERIAL_CONTROL_FLAG val) {
  const char *s = SERIAL_CONTROL_FLAG_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "SERIAL_CONTROL_FLAG<%d>", (int)val);
}

static const char *MAV_DISTANCE_SENSOR_str(enum MAV_DISTANCE_SENSOR val) {
  switch (val) {
  case MAV_DISTANCE_SENSOR_LASER:
    return "MAV_DISTANCE_SENSOR_LASER";
  case MAV_DISTANCE_SENSOR_ULTRASOUND:
    return "MAV_DISTANCE_SENSOR_ULTRASOUND";
  case MAV_DISTANCE_SENSOR_INFRARED:
    return "MAV_DISTANCE_SENSOR_INFRARED";
  case MAV_DISTANCE_SENSOR_RADAR:
    return "MAV_DISTANCE_SENSOR_RADAR";
  case MAV_DISTANCE_SENSOR_UNKNOWN:
    return "MAV_DISTANCE_SENSOR_UNKNOWN";
  }
  return NULL;
};

static size_t psnprint_MAV_DISTANCE_SENSOR(char **p, size_t *sz,
                                           enum MAV_DISTANCE_SENSOR val) {
  const char *s = MAV_DISTANCE_SENSOR_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_DISTANCE_SENSOR<%d>", (int)val);
}

static const char *MAV_SENSOR_ORIENTATION_str(enum MAV_SENSOR_ORIENTATION val) {
  switch (val) {
  case MAV_SENSOR_ROTATION_NONE:
    return "MAV_SENSOR_ROTATION_NONE";
  case MAV_SENSOR_ROTATION_YAW_45:
    return "MAV_SENSOR_ROTATION_YAW_45";
  case MAV_SENSOR_ROTATION_YAW_90:
    return "MAV_SENSOR_ROTATION_YAW_90";
  case MAV_SENSOR_ROTATION_YAW_135:
    return "MAV_SENSOR_ROTATION_YAW_135";
  case MAV_SENSOR_ROTATION_YAW_180:
    return "MAV_SENSOR_ROTATION_YAW_180";
  case MAV_SENSOR_ROTATION_YAW_225:
    return "MAV_SENSOR_ROTATION_YAW_225";
  case MAV_SENSOR_ROTATION_YAW_270:
    return "MAV_SENSOR_ROTATION_YAW_270";
  case MAV_SENSOR_ROTATION_YAW_315:
    return "MAV_SENSOR_ROTATION_YAW_315";
  case MAV_SENSOR_ROTATION_ROLL_180:
    return "MAV_SENSOR_ROTATION_ROLL_180";
  case MAV_SENSOR_ROTATION_ROLL_180_YAW_45:
    return "MAV_SENSOR_ROTATION_ROLL_180_YAW_45";
  case MAV_SENSOR_ROTATION_ROLL_180_YAW_90:
    return "MAV_SENSOR_ROTATION_ROLL_180_YAW_90";
  case MAV_SENSOR_ROTATION_ROLL_180_YAW_135:
    return "MAV_SENSOR_ROTATION_ROLL_180_YAW_135";
  case MAV_SENSOR_ROTATION_PITCH_180:
    return "MAV_SENSOR_ROTATION_PITCH_180";
  case MAV_SENSOR_ROTATION_ROLL_180_YAW_225:
    return "MAV_SENSOR_ROTATION_ROLL_180_YAW_225";
  case MAV_SENSOR_ROTATION_ROLL_180_YAW_270:
    return "MAV_SENSOR_ROTATION_ROLL_180_YAW_270";
  case MAV_SENSOR_ROTATION_ROLL_180_YAW_315:
    return "MAV_SENSOR_ROTATION_ROLL_180_YAW_315";
  case MAV_SENSOR_ROTATION_ROLL_90:
    return "MAV_SENSOR_ROTATION_ROLL_90";
  case MAV_SENSOR_ROTATION_ROLL_90_YAW_45:
    return "MAV_SENSOR_ROTATION_ROLL_90_YAW_45";
  case MAV_SENSOR_ROTATION_ROLL_90_YAW_90:
    return "MAV_SENSOR_ROTATION_ROLL_90_YAW_90";
  case MAV_SENSOR_ROTATION_ROLL_90_YAW_135:
    return "MAV_SENSOR_ROTATION_ROLL_90_YAW_135";
  case MAV_SENSOR_ROTATION_ROLL_270:
    return "MAV_SENSOR_ROTATION_ROLL_270";
  case MAV_SENSOR_ROTATION_ROLL_270_YAW_45:
    return "MAV_SENSOR_ROTATION_ROLL_270_YAW_45";
  case MAV_SENSOR_ROTATION_ROLL_270_YAW_90:
    return "MAV_SENSOR_ROTATION_ROLL_270_YAW_90";
  case MAV_SENSOR_ROTATION_ROLL_270_YAW_135:
    return "MAV_SENSOR_ROTATION_ROLL_270_YAW_135";
  case MAV_SENSOR_ROTATION_PITCH_90:
    return "MAV_SENSOR_ROTATION_PITCH_90";
  case MAV_SENSOR_ROTATION_PITCH_270:
    return "MAV_SENSOR_ROTATION_PITCH_270";
  case MAV_SENSOR_ROTATION_PITCH_180_YAW_90:
    return "MAV_SENSOR_ROTATION_PITCH_180_YAW_90";
  case MAV_SENSOR_ROTATION_PITCH_180_YAW_270:
    return "MAV_SENSOR_ROTATION_PITCH_180_YAW_270";
  case MAV_SENSOR_ROTATION_ROLL_90_PITCH_90:
    return "MAV_SENSOR_ROTATION_ROLL_90_PITCH_90";
  case MAV_SENSOR_ROTATION_ROLL_180_PITCH_90:
    return "MAV_SENSOR_ROTATION_ROLL_180_PITCH_90";
  case MAV_SENSOR_ROTATION_ROLL_270_PITCH_90:
    return "MAV_SENSOR_ROTATION_ROLL_270_PITCH_90";
  case MAV_SENSOR_ROTATION_ROLL_90_PITCH_180:
    return "MAV_SENSOR_ROTATION_ROLL_90_PITCH_180";
  case MAV_SENSOR_ROTATION_ROLL_270_PITCH_180:
    return "MAV_SENSOR_ROTATION_ROLL_270_PITCH_180";
  case MAV_SENSOR_ROTATION_ROLL_90_PITCH_270:
    return "MAV_SENSOR_ROTATION_ROLL_90_PITCH_270";
  case MAV_SENSOR_ROTATION_ROLL_180_PITCH_270:
    return "MAV_SENSOR_ROTATION_ROLL_180_PITCH_270";
  case MAV_SENSOR_ROTATION_ROLL_270_PITCH_270:
    return "MAV_SENSOR_ROTATION_ROLL_270_PITCH_270";
  case MAV_SENSOR_ROTATION_ROLL_90_PITCH_180_YAW_90:
    return "MAV_SENSOR_ROTATION_ROLL_90_PITCH_180_YAW_90";
  case MAV_SENSOR_ROTATION_ROLL_90_YAW_270:
    return "MAV_SENSOR_ROTATION_ROLL_90_YAW_270";
  case MAV_SENSOR_ROTATION_ROLL_90_PITCH_68_YAW_293:
    return "MAV_SENSOR_ROTATION_ROLL_90_PITCH_68_YAW_293";
  case MAV_SENSOR_ROTATION_PITCH_315:
    return "MAV_SENSOR_ROTATION_PITCH_315";
  case MAV_SENSOR_ROTATION_ROLL_90_PITCH_315:
    return "MAV_SENSOR_ROTATION_ROLL_90_PITCH_315";
  case MAV_SENSOR_ROTATION_CUSTOM:
    return "MAV_SENSOR_ROTATION_CUSTOM";
  }
  return NULL;
};

static size_t psnprint_MAV_SENSOR_ORIENTATION(char **p, size_t *sz,
                                              enum MAV_SENSOR_ORIENTATION val) {
  const char *s = MAV_SENSOR_ORIENTATION_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_SENSOR_ORIENTATION<%d>", (int)val);
}

static const char *
MAV_PROTOCOL_CAPABILITY_str(enum MAV_PROTOCOL_CAPABILITY val) {
  switch (val) {
  case MAV_PROTOCOL_CAPABILITY_MISSION_FLOAT:
    return "MAV_PROTOCOL_CAPABILITY_MISSION_FLOAT";
  case MAV_PROTOCOL_CAPABILITY_PARAM_FLOAT:
    return "MAV_PROTOCOL_CAPABILITY_PARAM_FLOAT";
  case MAV_PROTOCOL_CAPABILITY_MISSION_INT:
    return "MAV_PROTOCOL_CAPABILITY_MISSION_INT";
  case MAV_PROTOCOL_CAPABILITY_COMMAND_INT:
    return "MAV_PROTOCOL_CAPABILITY_COMMAND_INT";
  case MAV_PROTOCOL_CAPABILITY_PARAM_UNION:
    return "MAV_PROTOCOL_CAPABILITY_PARAM_UNION";
  case MAV_PROTOCOL_CAPABILITY_FTP:
    return "MAV_PROTOCOL_CAPABILITY_FTP";
  case MAV_PROTOCOL_CAPABILITY_SET_ATTITUDE_TARGET:
    return "MAV_PROTOCOL_CAPABILITY_SET_ATTITUDE_TARGET";
  case MAV_PROTOCOL_CAPABILITY_SET_POSITION_TARGET_LOCAL_NED:
    return "MAV_PROTOCOL_CAPABILITY_SET_POSITION_TARGET_LOCAL_NED";
  case MAV_PROTOCOL_CAPABILITY_SET_POSITION_TARGET_GLOBAL_INT:
    return "MAV_PROTOCOL_CAPABILITY_SET_POSITION_TARGET_GLOBAL_INT";
  case MAV_PROTOCOL_CAPABILITY_TERRAIN:
    return "MAV_PROTOCOL_CAPABILITY_TERRAIN";
  case MAV_PROTOCOL_CAPABILITY_SET_ACTUATOR_TARGET:
    return "MAV_PROTOCOL_CAPABILITY_SET_ACTUATOR_TARGET";
  case MAV_PROTOCOL_CAPABILITY_FLIGHT_TERMINATION:
    return "MAV_PROTOCOL_CAPABILITY_FLIGHT_TERMINATION";
  case MAV_PROTOCOL_CAPABILITY_COMPASS_CALIBRATION:
    return "MAV_PROTOCOL_CAPABILITY_COMPASS_CALIBRATION";
  case MAV_PROTOCOL_CAPABILITY_MAVLINK2:
    return "MAV_PROTOCOL_CAPABILITY_MAVLINK2";
  case MAV_PROTOCOL_CAPABILITY_MISSION_FENCE:
    return "MAV_PROTOCOL_CAPABILITY_MISSION_FENCE";
  case MAV_PROTOCOL_CAPABILITY_MISSION_RALLY:
    return "MAV_PROTOCOL_CAPABILITY_MISSION_RALLY";
  case MAV_PROTOCOL_CAPABILITY_FLIGHT_INFORMATION:
    return "MAV_PROTOCOL_CAPABILITY_FLIGHT_INFORMATION";
  }
  return NULL;
};

static size_t
psnprint_MAV_PROTOCOL_CAPABILITY(char **p, size_t *sz,
                                 enum MAV_PROTOCOL_CAPABILITY val) {
  const char *s = MAV_PROTOCOL_CAPABILITY_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_PROTOCOL_CAPABILITY<%d>", (int)val);
}

static const char *MAV_MISSION_TYPE_str(enum MAV_MISSION_TYPE val) {
  switch (val) {
  case MAV_MISSION_TYPE_MISSION:
    return "MAV_MISSION_TYPE_MISSION";
  case MAV_MISSION_TYPE_FENCE:
    return "MAV_MISSION_TYPE_FENCE";
  case MAV_MISSION_TYPE_RALLY:
    return "MAV_MISSION_TYPE_RALLY";
  case MAV_MISSION_TYPE_ALL:
    return "MAV_MISSION_TYPE_ALL";
  }
  return NULL;
};

static size_t psnprint_MAV_MISSION_TYPE(char **p, size_t *sz,
                                        enum MAV_MISSION_TYPE val) {
  const char *s = MAV_MISSION_TYPE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_MISSION_TYPE<%d>", (int)val);
}

static const char *MAV_ESTIMATOR_TYPE_str(enum MAV_ESTIMATOR_TYPE val) {
  switch (val) {
  case MAV_ESTIMATOR_TYPE_NAIVE:
    return "MAV_ESTIMATOR_TYPE_NAIVE";
  case MAV_ESTIMATOR_TYPE_VISION:
    return "MAV_ESTIMATOR_TYPE_VISION";
  case MAV_ESTIMATOR_TYPE_VIO:
    return "MAV_ESTIMATOR_TYPE_VIO";
  case MAV_ESTIMATOR_TYPE_GPS:
    return "MAV_ESTIMATOR_TYPE_GPS";
  case MAV_ESTIMATOR_TYPE_GPS_INS:
    return "MAV_ESTIMATOR_TYPE_GPS_INS";
  }
  return NULL;
};

static size_t psnprint_MAV_ESTIMATOR_TYPE(char **p, size_t *sz,
                                          enum MAV_ESTIMATOR_TYPE val) {
  const char *s = MAV_ESTIMATOR_TYPE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_ESTIMATOR_TYPE<%d>", (int)val);
}

static const char *MAV_BATTERY_TYPE_str(enum MAV_BATTERY_TYPE val) {
  switch (val) {
  case MAV_BATTERY_TYPE_UNKNOWN:
    return "MAV_BATTERY_TYPE_UNKNOWN";
  case MAV_BATTERY_TYPE_LIPO:
    return "MAV_BATTERY_TYPE_LIPO";
  case MAV_BATTERY_TYPE_LIFE:
    return "MAV_BATTERY_TYPE_LIFE";
  case MAV_BATTERY_TYPE_LION:
    return "MAV_BATTERY_TYPE_LION";
  case MAV_BATTERY_TYPE_NIMH:
    return "MAV_BATTERY_TYPE_NIMH";
  }
  return NULL;
};

static size_t psnprint_MAV_BATTERY_TYPE(char **p, size_t *sz,
                                        enum MAV_BATTERY_TYPE val) {
  const char *s = MAV_BATTERY_TYPE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_BATTERY_TYPE<%d>", (int)val);
}

static const char *MAV_BATTERY_FUNCTION_str(enum MAV_BATTERY_FUNCTION val) {
  switch (val) {
  case MAV_BATTERY_FUNCTION_UNKNOWN:
    return "MAV_BATTERY_FUNCTION_UNKNOWN";
  case MAV_BATTERY_FUNCTION_ALL:
    return "MAV_BATTERY_FUNCTION_ALL";
  case MAV_BATTERY_FUNCTION_PROPULSION:
    return "MAV_BATTERY_FUNCTION_PROPULSION";
  case MAV_BATTERY_FUNCTION_AVIONICS:
    return "MAV_BATTERY_FUNCTION_AVIONICS";
  case MAV_BATTERY_TYPE_PAYLOAD:
    return "MAV_BATTERY_TYPE_PAYLOAD";
  }
  return NULL;
};

static size_t psnprint_MAV_BATTERY_FUNCTION(char **p, size_t *sz,
                                            enum MAV_BATTERY_FUNCTION val) {
  const char *s = MAV_BATTERY_FUNCTION_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_BATTERY_FUNCTION<%d>", (int)val);
}

static const char *
MAV_BATTERY_CHARGE_STATE_str(enum MAV_BATTERY_CHARGE_STATE val) {
  switch (val) {
  case MAV_BATTERY_CHARGE_STATE_UNDEFINED:
    return "MAV_BATTERY_CHARGE_STATE_UNDEFINED";
  case MAV_BATTERY_CHARGE_STATE_OK:
    return "MAV_BATTERY_CHARGE_STATE_OK";
  case MAV_BATTERY_CHARGE_STATE_LOW:
    return "MAV_BATTERY_CHARGE_STATE_LOW";
  case MAV_BATTERY_CHARGE_STATE_CRITICAL:
    return "MAV_BATTERY_CHARGE_STATE_CRITICAL";
  case MAV_BATTERY_CHARGE_STATE_EMERGENCY:
    return "MAV_BATTERY_CHARGE_STATE_EMERGENCY";
  case MAV_BATTERY_CHARGE_STATE_FAILED:
    return "MAV_BATTERY_CHARGE_STATE_FAILED";
  case MAV_BATTERY_CHARGE_STATE_UNHEALTHY:
    return "MAV_BATTERY_CHARGE_STATE_UNHEALTHY";
  case MAV_BATTERY_CHARGE_STATE_CHARGING:
    return "MAV_BATTERY_CHARGE_STATE_CHARGING";
  }
  return NULL;
};

static size_t
psnprint_MAV_BATTERY_CHARGE_STATE(char **p, size_t *sz,
                                  enum MAV_BATTERY_CHARGE_STATE val) {
  const char *s = MAV_BATTERY_CHARGE_STATE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_BATTERY_CHARGE_STATE<%d>", (int)val);
}

static const char *
MAV_SMART_BATTERY_FAULT_str(enum MAV_SMART_BATTERY_FAULT val) {
  switch (val) {
  case MAV_SMART_BATTERY_FAULT_DEEP_DISCHARGE:
    return "MAV_SMART_BATTERY_FAULT_DEEP_DISCHARGE";
  case MAV_SMART_BATTERY_FAULT_SPIKES:
    return "MAV_SMART_BATTERY_FAULT_SPIKES";
  case MAV_SMART_BATTERY_FAULT_SINGLE_CELL_FAIL:
    return "MAV_SMART_BATTERY_FAULT_SINGLE_CELL_FAIL";
  case MAV_SMART_BATTERY_FAULT_OVER_CURRENT:
    return "MAV_SMART_BATTERY_FAULT_OVER_CURRENT";
  case MAV_SMART_BATTERY_FAULT_OVER_TEMPERATURE:
    return "MAV_SMART_BATTERY_FAULT_OVER_TEMPERATURE";
  case MAV_SMART_BATTERY_FAULT_UNDER_TEMPERATURE:
    return "MAV_SMART_BATTERY_FAULT_UNDER_TEMPERATURE";
  }
  return NULL;
};

static size_t
psnprint_MAV_SMART_BATTERY_FAULT(char **p, size_t *sz,
                                 enum MAV_SMART_BATTERY_FAULT val) {
  const char *s = MAV_SMART_BATTERY_FAULT_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_SMART_BATTERY_FAULT<%d>", (int)val);
}

static const char *MAV_VTOL_STATE_str(enum MAV_VTOL_STATE val) {
  switch (val) {
  case MAV_VTOL_STATE_UNDEFINED:
    return "MAV_VTOL_STATE_UNDEFINED";
  case MAV_VTOL_STATE_TRANSITION_TO_FW:
    return "MAV_VTOL_STATE_TRANSITION_TO_FW";
  case MAV_VTOL_STATE_TRANSITION_TO_MC:
    return "MAV_VTOL_STATE_TRANSITION_TO_MC";
  case MAV_VTOL_STATE_MC:
    return "MAV_VTOL_STATE_MC";
  case MAV_VTOL_STATE_FW:
    return "MAV_VTOL_STATE_FW";
  }
  return NULL;
};

static size_t psnprint_MAV_VTOL_STATE(char **p, size_t *sz,
                                      enum MAV_VTOL_STATE val) {
  const char *s = MAV_VTOL_STATE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_VTOL_STATE<%d>", (int)val);
}

static const char *MAV_LANDED_STATE_str(enum MAV_LANDED_STATE val) {
  switch (val) {
  case MAV_LANDED_STATE_UNDEFINED:
    return "MAV_LANDED_STATE_UNDEFINED";
  case MAV_LANDED_STATE_ON_GROUND:
    return "MAV_LANDED_STATE_ON_GROUND";
  case MAV_LANDED_STATE_IN_AIR:
    return "MAV_LANDED_STATE_IN_AIR";
  case MAV_LANDED_STATE_TAKEOFF:
    return "MAV_LANDED_STATE_TAKEOFF";
  case MAV_LANDED_STATE_LANDING:
    return "MAV_LANDED_STATE_LANDING";
  }
  return NULL;
};

static size_t psnprint_MAV_LANDED_STATE(char **p, size_t *sz,
                                        enum MAV_LANDED_STATE val) {
  const char *s = MAV_LANDED_STATE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_LANDED_STATE<%d>", (int)val);
}

static const char *ADSB_ALTITUDE_TYPE_str(enum ADSB_ALTITUDE_TYPE val) {
  switch (val) {
  case ADSB_ALTITUDE_TYPE_PRESSURE_QNH:
    return "ADSB_ALTITUDE_TYPE_PRESSURE_QNH";
  case ADSB_ALTITUDE_TYPE_GEOMETRIC:
    return "ADSB_ALTITUDE_TYPE_GEOMETRIC";
  }
  return NULL;
};

static size_t psnprint_ADSB_ALTITUDE_TYPE(char **p, size_t *sz,
                                          enum ADSB_ALTITUDE_TYPE val) {
  const char *s = ADSB_ALTITUDE_TYPE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "ADSB_ALTITUDE_TYPE<%d>", (int)val);
}

static const char *ADSB_EMITTER_TYPE_str(enum ADSB_EMITTER_TYPE val) {
  switch (val) {
  case ADSB_EMITTER_TYPE_NO_INFO:
    return "ADSB_EMITTER_TYPE_NO_INFO";
  case ADSB_EMITTER_TYPE_LIGHT:
    return "ADSB_EMITTER_TYPE_LIGHT";
  case ADSB_EMITTER_TYPE_SMALL:
    return "ADSB_EMITTER_TYPE_SMALL";
  case ADSB_EMITTER_TYPE_LARGE:
    return "ADSB_EMITTER_TYPE_LARGE";
  case ADSB_EMITTER_TYPE_HIGH_VORTEX_LARGE:
    return "ADSB_EMITTER_TYPE_HIGH_VORTEX_LARGE";
  case ADSB_EMITTER_TYPE_HEAVY:
    return "ADSB_EMITTER_TYPE_HEAVY";
  case ADSB_EMITTER_TYPE_HIGHLY_MANUV:
    return "ADSB_EMITTER_TYPE_HIGHLY_MANUV";
  case ADSB_EMITTER_TYPE_ROTOCRAFT:
    return "ADSB_EMITTER_TYPE_ROTOCRAFT";
  case ADSB_EMITTER_TYPE_UNASSIGNED:
    return "ADSB_EMITTER_TYPE_UNASSIGNED";
  case ADSB_EMITTER_TYPE_GLIDER:
    return "ADSB_EMITTER_TYPE_GLIDER";
  case ADSB_EMITTER_TYPE_LIGHTER_AIR:
    return "ADSB_EMITTER_TYPE_LIGHTER_AIR";
  case ADSB_EMITTER_TYPE_PARACHUTE:
    return "ADSB_EMITTER_TYPE_PARACHUTE";
  case ADSB_EMITTER_TYPE_ULTRA_LIGHT:
    return "ADSB_EMITTER_TYPE_ULTRA_LIGHT";
  case ADSB_EMITTER_TYPE_UNASSIGNED2:
    return "ADSB_EMITTER_TYPE_UNASSIGNED2";
  case ADSB_EMITTER_TYPE_UAV:
    return "ADSB_EMITTER_TYPE_UAV";
  case ADSB_EMITTER_TYPE_SPACE:
    return "ADSB_EMITTER_TYPE_SPACE";
  case ADSB_EMITTER_TYPE_UNASSGINED3:
    return "ADSB_EMITTER_TYPE_UNASSGINED3";
  case ADSB_EMITTER_TYPE_EMERGENCY_SURFACE:
    return "ADSB_EMITTER_TYPE_EMERGENCY_SURFACE";
  case ADSB_EMITTER_TYPE_SERVICE_SURFACE:
    return "ADSB_EMITTER_TYPE_SERVICE_SURFACE";
  case ADSB_EMITTER_TYPE_POINT_OBSTACLE:
    return "ADSB_EMITTER_TYPE_POINT_OBSTACLE";
  }
  return NULL;
};

static size_t psnprint_ADSB_EMITTER_TYPE(char **p, size_t *sz,
                                         enum ADSB_EMITTER_TYPE val) {
  const char *s = ADSB_EMITTER_TYPE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "ADSB_EMITTER_TYPE<%d>", (int)val);
}

static const char *ADSB_FLAGS_str(enum ADSB_FLAGS val) {
  switch (val) {
  case ADSB_FLAGS_VALID_COORDS:
    return "ADSB_FLAGS_VALID_COORDS";
  case ADSB_FLAGS_VALID_ALTITUDE:
    return "ADSB_FLAGS_VALID_ALTITUDE";
  case ADSB_FLAGS_VALID_HEADING:
    return "ADSB_FLAGS_VALID_HEADING";
  case ADSB_FLAGS_VALID_VELOCITY:
    return "ADSB_FLAGS_VALID_VELOCITY";
  case ADSB_FLAGS_VALID_CALLSIGN:
    return "ADSB_FLAGS_VALID_CALLSIGN";
  case ADSB_FLAGS_VALID_SQUAWK:
    return "ADSB_FLAGS_VALID_SQUAWK";
  case ADSB_FLAGS_SIMULATED:
    return "ADSB_FLAGS_SIMULATED";
  }
  return NULL;
};

static size_t psnprint_ADSB_FLAGS(char **p, size_t *sz, enum ADSB_FLAGS val) {
  const char *s = ADSB_FLAGS_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "ADSB_FLAGS<%d>", (int)val);
}

static const char *
MAV_DO_REPOSITION_FLAGS_str(enum MAV_DO_REPOSITION_FLAGS val) {
  switch (val) {
  case MAV_DO_REPOSITION_FLAGS_CHANGE_MODE:
    return "MAV_DO_REPOSITION_FLAGS_CHANGE_MODE";
  }
  return NULL;
};

static size_t
psnprint_MAV_DO_REPOSITION_FLAGS(char **p, size_t *sz,
                                 enum MAV_DO_REPOSITION_FLAGS val) {
  const char *s = MAV_DO_REPOSITION_FLAGS_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_DO_REPOSITION_FLAGS<%d>", (int)val);
}

static const char *ESTIMATOR_STATUS_FLAGS_str(enum ESTIMATOR_STATUS_FLAGS val) {
  switch (val) {
  case ESTIMATOR_ATTITUDE:
    return "ESTIMATOR_ATTITUDE";
  case ESTIMATOR_VELOCITY_HORIZ:
    return "ESTIMATOR_VELOCITY_HORIZ";
  case ESTIMATOR_VELOCITY_VERT:
    return "ESTIMATOR_VELOCITY_VERT";
  case ESTIMATOR_POS_HORIZ_REL:
    return "ESTIMATOR_POS_HORIZ_REL";
  case ESTIMATOR_POS_HORIZ_ABS:
    return "ESTIMATOR_POS_HORIZ_ABS";
  case ESTIMATOR_POS_VERT_ABS:
    return "ESTIMATOR_POS_VERT_ABS";
  case ESTIMATOR_POS_VERT_AGL:
    return "ESTIMATOR_POS_VERT_AGL";
  case ESTIMATOR_CONST_POS_MODE:
    return "ESTIMATOR_CONST_POS_MODE";
  case ESTIMATOR_PRED_POS_HORIZ_REL:
    return "ESTIMATOR_PRED_POS_HORIZ_REL";
  case ESTIMATOR_PRED_POS_HORIZ_ABS:
    return "ESTIMATOR_PRED_POS_HORIZ_ABS";
  case ESTIMATOR_GPS_GLITCH:
    return "ESTIMATOR_GPS_GLITCH";
  case ESTIMATOR_ACCEL_ERROR:
    return "ESTIMATOR_ACCEL_ERROR";
  }
  return NULL;
};

static size_t psnprint_ESTIMATOR_STATUS_FLAGS(char **p, size_t *sz,
                                              enum ESTIMATOR_STATUS_FLAGS val) {
  const char *s = ESTIMATOR_STATUS_FLAGS_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "ESTIMATOR_STATUS_FLAGS<%d>", (int)val);
}

static const char *MOTOR_TEST_ORDER_str(enum MOTOR_TEST_ORDER val) {
  switch (val) {
  case MOTOR_TEST_ORDER_DEFAULT:
    return "MOTOR_TEST_ORDER_DEFAULT";
  case MOTOR_TEST_ORDER_SEQUENCE:
    return "MOTOR_TEST_ORDER_SEQUENCE";
  case MOTOR_TEST_ORDER_BOARD:
    return "MOTOR_TEST_ORDER_BOARD";
  }
  return NULL;
};

static size_t psnprint_MOTOR_TEST_ORDER(char **p, size_t *sz,
                                        enum MOTOR_TEST_ORDER val) {
  const char *s = MOTOR_TEST_ORDER_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MOTOR_TEST_ORDER<%d>", (int)val);
}

static const char *
MOTOR_TEST_THROTTLE_TYPE_str(enum MOTOR_TEST_THROTTLE_TYPE val) {
  switch (val) {
  case MOTOR_TEST_THROTTLE_PERCENT:
    return "MOTOR_TEST_THROTTLE_PERCENT";
  case MOTOR_TEST_THROTTLE_PWM:
    return "MOTOR_TEST_THROTTLE_PWM";
  case MOTOR_TEST_THROTTLE_PILOT:
    return "MOTOR_TEST_THROTTLE_PILOT";
  case MOTOR_TEST_COMPASS_CAL:
    return "MOTOR_TEST_COMPASS_CAL";
  }
  return NULL;
};

static size_t
psnprint_MOTOR_TEST_THROTTLE_TYPE(char **p, size_t *sz,
                                  enum MOTOR_TEST_THROTTLE_TYPE val) {
  const char *s = MOTOR_TEST_THROTTLE_TYPE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MOTOR_TEST_THROTTLE_TYPE<%d>", (int)val);
}

static const char *GPS_INPUT_IGNORE_FLAGS_str(enum GPS_INPUT_IGNORE_FLAGS val) {
  switch (val) {
  case GPS_INPUT_IGNORE_FLAG_ALT:
    return "GPS_INPUT_IGNORE_FLAG_ALT";
  case GPS_INPUT_IGNORE_FLAG_HDOP:
    return "GPS_INPUT_IGNORE_FLAG_HDOP";
  case GPS_INPUT_IGNORE_FLAG_VDOP:
    return "GPS_INPUT_IGNORE_FLAG_VDOP";
  case GPS_INPUT_IGNORE_FLAG_VEL_HORIZ:
    return "GPS_INPUT_IGNORE_FLAG_VEL_HORIZ";
  case GPS_INPUT_IGNORE_FLAG_VEL_VERT:
    return "GPS_INPUT_IGNORE_FLAG_VEL_VERT";
  case GPS_INPUT_IGNORE_FLAG_SPEED_ACCURACY:
    return "GPS_INPUT_IGNORE_FLAG_SPEED_ACCURACY";
  case GPS_INPUT_IGNORE_FLAG_HORIZONTAL_ACCURACY:
    return "GPS_INPUT_IGNORE_FLAG_HORIZONTAL_ACCURACY";
  case GPS_INPUT_IGNORE_FLAG_VERTICAL_ACCURACY:
    return "GPS_INPUT_IGNORE_FLAG_VERTICAL_ACCURACY";
  }
  return NULL;
};

static size_t psnprint_GPS_INPUT_IGNORE_FLAGS(char **p, size_t *sz,
                                              enum GPS_INPUT_IGNORE_FLAGS val) {
  const char *s = GPS_INPUT_IGNORE_FLAGS_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "GPS_INPUT_IGNORE_FLAGS<%d>", (int)val);
}

static const char *MAV_COLLISION_ACTION_str(enum MAV_COLLISION_ACTION val) {
  switch (val) {
  case MAV_COLLISION_ACTION_NONE:
    return "MAV_COLLISION_ACTION_NONE";
  case MAV_COLLISION_ACTION_REPORT:
    return "MAV_COLLISION_ACTION_REPORT";
  case MAV_COLLISION_ACTION_ASCEND_OR_DESCEND:
    return "MAV_COLLISION_ACTION_ASCEND_OR_DESCEND";
  case MAV_COLLISION_ACTION_MOVE_HORIZONTALLY:
    return "MAV_COLLISION_ACTION_MOVE_HORIZONTALLY";
  case MAV_COLLISION_ACTION_MOVE_PERPENDICULAR:
    return "MAV_COLLISION_ACTION_MOVE_PERPENDICULAR";
  case MAV_COLLISION_ACTION_RTL:
    return "MAV_COLLISION_ACTION_RTL";
  case MAV_COLLISION_ACTION_HOVER:
    return "MAV_COLLISION_ACTION_HOVER";
  }
  return NULL;
};

static size_t psnprint_MAV_COLLISION_ACTION(char **p, size_t *sz,
                                            enum MAV_COLLISION_ACTION val) {
  const char *s = MAV_COLLISION_ACTION_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_COLLISION_ACTION<%d>", (int)val);
}

static const char *
MAV_COLLISION_THREAT_LEVEL_str(enum MAV_COLLISION_THREAT_LEVEL val) {
  switch (val) {
  case MAV_COLLISION_THREAT_LEVEL_NONE:
    return "MAV_COLLISION_THREAT_LEVEL_NONE";
  case MAV_COLLISION_THREAT_LEVEL_LOW:
    return "MAV_COLLISION_THREAT_LEVEL_LOW";
  case MAV_COLLISION_THREAT_LEVEL_HIGH:
    return "MAV_COLLISION_THREAT_LEVEL_HIGH";
  }
  return NULL;
};

static size_t
psnprint_MAV_COLLISION_THREAT_LEVEL(char **p, size_t *sz,
                                    enum MAV_COLLISION_THREAT_LEVEL val) {
  const char *s = MAV_COLLISION_THREAT_LEVEL_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_COLLISION_THREAT_LEVEL<%d>", (int)val);
}

static const char *MAV_COLLISION_SRC_str(enum MAV_COLLISION_SRC val) {
  switch (val) {
  case MAV_COLLISION_SRC_ADSB:
    return "MAV_COLLISION_SRC_ADSB";
  case MAV_COLLISION_SRC_MAVLINK_GPS_GLOBAL_INT:
    return "MAV_COLLISION_SRC_MAVLINK_GPS_GLOBAL_INT";
  }
  return NULL;
};

static size_t psnprint_MAV_COLLISION_SRC(char **p, size_t *sz,
                                         enum MAV_COLLISION_SRC val) {
  const char *s = MAV_COLLISION_SRC_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_COLLISION_SRC<%d>", (int)val);
}

static const char *GPS_FIX_TYPE_str(enum GPS_FIX_TYPE val) {
  switch (val) {
  case GPS_FIX_TYPE_NO_GPS:
    return "GPS_FIX_TYPE_NO_GPS";
  case GPS_FIX_TYPE_NO_FIX:
    return "GPS_FIX_TYPE_NO_FIX";
  case GPS_FIX_TYPE_2D_FIX:
    return "GPS_FIX_TYPE_2D_FIX";
  case GPS_FIX_TYPE_3D_FIX:
    return "GPS_FIX_TYPE_3D_FIX";
  case GPS_FIX_TYPE_DGPS:
    return "GPS_FIX_TYPE_DGPS";
  case GPS_FIX_TYPE_RTK_FLOAT:
    return "GPS_FIX_TYPE_RTK_FLOAT";
  case GPS_FIX_TYPE_RTK_FIXED:
    return "GPS_FIX_TYPE_RTK_FIXED";
  case GPS_FIX_TYPE_STATIC:
    return "GPS_FIX_TYPE_STATIC";
  case GPS_FIX_TYPE_PPP:
    return "GPS_FIX_TYPE_PPP";
  }
  return NULL;
};

static size_t psnprint_GPS_FIX_TYPE(char **p, size_t *sz,
                                    enum GPS_FIX_TYPE val) {
  const char *s = GPS_FIX_TYPE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "GPS_FIX_TYPE<%d>", (int)val);
}

static const char *
RTK_BASELINE_COORDINATE_SYSTEM_str(enum RTK_BASELINE_COORDINATE_SYSTEM val) {
  switch (val) {
  case RTK_BASELINE_COORDINATE_SYSTEM_ECEF:
    return "RTK_BASELINE_COORDINATE_SYSTEM_ECEF";
  case RTK_BASELINE_COORDINATE_SYSTEM_NED:
    return "RTK_BASELINE_COORDINATE_SYSTEM_NED";
  }
  return NULL;
};

static size_t psnprint_RTK_BASELINE_COORDINATE_SYSTEM(
    char **p, size_t *sz, enum RTK_BASELINE_COORDINATE_SYSTEM val) {
  const char *s = RTK_BASELINE_COORDINATE_SYSTEM_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "RTK_BASELINE_COORDINATE_SYSTEM<%d>", (int)val);
}

static const char *LANDING_TARGET_TYPE_str(enum LANDING_TARGET_TYPE val) {
  switch (val) {
  case LANDING_TARGET_TYPE_LIGHT_BEACON:
    return "LANDING_TARGET_TYPE_LIGHT_BEACON";
  case LANDING_TARGET_TYPE_RADIO_BEACON:
    return "LANDING_TARGET_TYPE_RADIO_BEACON";
  case LANDING_TARGET_TYPE_VISION_FIDUCIAL:
    return "LANDING_TARGET_TYPE_VISION_FIDUCIAL";
  case LANDING_TARGET_TYPE_VISION_OTHER:
    return "LANDING_TARGET_TYPE_VISION_OTHER";
  }
  return NULL;
};

static size_t psnprint_LANDING_TARGET_TYPE(char **p, size_t *sz,
                                           enum LANDING_TARGET_TYPE val) {
  const char *s = LANDING_TARGET_TYPE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "LANDING_TARGET_TYPE<%d>", (int)val);
}

static const char *
VTOL_TRANSITION_HEADING_str(enum VTOL_TRANSITION_HEADING val) {
  switch (val) {
  case VTOL_TRANSITION_HEADING_VEHICLE_DEFAULT:
    return "VTOL_TRANSITION_HEADING_VEHICLE_DEFAULT";
  case VTOL_TRANSITION_HEADING_NEXT_WAYPOINT:
    return "VTOL_TRANSITION_HEADING_NEXT_WAYPOINT";
  case VTOL_TRANSITION_HEADING_TAKEOFF:
    return "VTOL_TRANSITION_HEADING_TAKEOFF";
  case VTOL_TRANSITION_HEADING_SPECIFIED:
    return "VTOL_TRANSITION_HEADING_SPECIFIED";
  case VTOL_TRANSITION_HEADING_ANY:
    return "VTOL_TRANSITION_HEADING_ANY";
  }
  return NULL;
};

static size_t
psnprint_VTOL_TRANSITION_HEADING(char **p, size_t *sz,
                                 enum VTOL_TRANSITION_HEADING val) {
  const char *s = VTOL_TRANSITION_HEADING_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "VTOL_TRANSITION_HEADING<%d>", (int)val);
}

static const char *CAMERA_CAP_FLAGS_str(enum CAMERA_CAP_FLAGS val) {
  switch (val) {
  case CAMERA_CAP_FLAGS_CAPTURE_VIDEO:
    return "CAMERA_CAP_FLAGS_CAPTURE_VIDEO";
  case CAMERA_CAP_FLAGS_CAPTURE_IMAGE:
    return "CAMERA_CAP_FLAGS_CAPTURE_IMAGE";
  case CAMERA_CAP_FLAGS_HAS_MODES:
    return "CAMERA_CAP_FLAGS_HAS_MODES";
  case CAMERA_CAP_FLAGS_CAN_CAPTURE_IMAGE_IN_VIDEO_MODE:
    return "CAMERA_CAP_FLAGS_CAN_CAPTURE_IMAGE_IN_VIDEO_MODE";
  case CAMERA_CAP_FLAGS_CAN_CAPTURE_VIDEO_IN_IMAGE_MODE:
    return "CAMERA_CAP_FLAGS_CAN_CAPTURE_VIDEO_IN_IMAGE_MODE";
  case CAMERA_CAP_FLAGS_HAS_IMAGE_SURVEY_MODE:
    return "CAMERA_CAP_FLAGS_HAS_IMAGE_SURVEY_MODE";
  case CAMERA_CAP_FLAGS_HAS_BASIC_ZOOM:
    return "CAMERA_CAP_FLAGS_HAS_BASIC_ZOOM";
  case CAMERA_CAP_FLAGS_HAS_BASIC_FOCUS:
    return "CAMERA_CAP_FLAGS_HAS_BASIC_FOCUS";
  case CAMERA_CAP_FLAGS_HAS_VIDEO_STREAM:
    return "CAMERA_CAP_FLAGS_HAS_VIDEO_STREAM";
  }
  return NULL;
};

static size_t psnprint_CAMERA_CAP_FLAGS(char **p, size_t *sz,
                                        enum CAMERA_CAP_FLAGS val) {
  const char *s = CAMERA_CAP_FLAGS_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "CAMERA_CAP_FLAGS<%d>", (int)val);
}

static const char *
VIDEO_STREAM_STATUS_FLAGS_str(enum VIDEO_STREAM_STATUS_FLAGS val) {
  switch (val) {
  case VIDEO_STREAM_STATUS_FLAGS_RUNNING:
    return "VIDEO_STREAM_STATUS_FLAGS_RUNNING";
  case VIDEO_STREAM_STATUS_FLAGS_THERMAL:
    return "VIDEO_STREAM_STATUS_FLAGS_THERMAL";
  }
  return NULL;
};

static size_t
psnprint_VIDEO_STREAM_STATUS_FLAGS(char **p, size_t *sz,
                                   enum VIDEO_STREAM_STATUS_FLAGS val) {
  const char *s = VIDEO_STREAM_STATUS_FLAGS_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "VIDEO_STREAM_STATUS_FLAGS<%d>", (int)val);
}

static const char *VIDEO_STREAM_TYPE_str(enum VIDEO_STREAM_TYPE val) {
  switch (val) {
  case VIDEO_STREAM_TYPE_RTSP:
    return "VIDEO_STREAM_TYPE_RTSP";
  case VIDEO_STREAM_TYPE_RTPUDP:
    return "VIDEO_STREAM_TYPE_RTPUDP";
  case VIDEO_STREAM_TYPE_TCP_MPEG:
    return "VIDEO_STREAM_TYPE_TCP_MPEG";
  case VIDEO_STREAM_TYPE_MPEG_TS_H264:
    return "VIDEO_STREAM_TYPE_MPEG_TS_H264";
  }
  return NULL;
};

static size_t psnprint_VIDEO_STREAM_TYPE(char **p, size_t *sz,
                                         enum VIDEO_STREAM_TYPE val) {
  const char *s = VIDEO_STREAM_TYPE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "VIDEO_STREAM_TYPE<%d>", (int)val);
}

static const char *CAMERA_ZOOM_TYPE_str(enum CAMERA_ZOOM_TYPE val) {
  switch (val) {
  case ZOOM_TYPE_STEP:
    return "ZOOM_TYPE_STEP";
  case ZOOM_TYPE_CONTINUOUS:
    return "ZOOM_TYPE_CONTINUOUS";
  case ZOOM_TYPE_RANGE:
    return "ZOOM_TYPE_RANGE";
  }
  return NULL;
};

static size_t psnprint_CAMERA_ZOOM_TYPE(char **p, size_t *sz,
                                        enum CAMERA_ZOOM_TYPE val) {
  const char *s = CAMERA_ZOOM_TYPE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "CAMERA_ZOOM_TYPE<%d>", (int)val);
}

static const char *SET_FOCUS_TYPE_str(enum SET_FOCUS_TYPE val) {
  switch (val) {
  case FOCUS_TYPE_STEP:
    return "FOCUS_TYPE_STEP";
  case FOCUS_TYPE_CONTINUOUS:
    return "FOCUS_TYPE_CONTINUOUS";
  case FOCUS_TYPE_RANGE:
    return "FOCUS_TYPE_RANGE";
  }
  return NULL;
};

static size_t psnprint_SET_FOCUS_TYPE(char **p, size_t *sz,
                                      enum SET_FOCUS_TYPE val) {
  const char *s = SET_FOCUS_TYPE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "SET_FOCUS_TYPE<%d>", (int)val);
}

static const char *PARAM_ACK_str(enum PARAM_ACK val) {
  switch (val) {
  case PARAM_ACK_ACCEPTED:
    return "PARAM_ACK_ACCEPTED";
  case PARAM_ACK_VALUE_UNSUPPORTED:
    return "PARAM_ACK_VALUE_UNSUPPORTED";
  case PARAM_ACK_FAILED:
    return "PARAM_ACK_FAILED";
  case PARAM_ACK_IN_PROGRESS:
    return "PARAM_ACK_IN_PROGRESS";
  }
  return NULL;
};

static size_t psnprint_PARAM_ACK(char **p, size_t *sz, enum PARAM_ACK val) {
  const char *s = PARAM_ACK_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "PARAM_ACK<%d>", (int)val);
}

static const char *CAMERA_MODE_str(enum CAMERA_MODE val) {
  switch (val) {
  case CAMERA_MODE_IMAGE:
    return "CAMERA_MODE_IMAGE";
  case CAMERA_MODE_VIDEO:
    return "CAMERA_MODE_VIDEO";
  case CAMERA_MODE_IMAGE_SURVEY:
    return "CAMERA_MODE_IMAGE_SURVEY";
  }
  return NULL;
};

static size_t psnprint_CAMERA_MODE(char **p, size_t *sz, enum CAMERA_MODE val) {
  const char *s = CAMERA_MODE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "CAMERA_MODE<%d>", (int)val);
}

static const char *
MAV_ARM_AUTH_DENIED_REASON_str(enum MAV_ARM_AUTH_DENIED_REASON val) {
  switch (val) {
  case MAV_ARM_AUTH_DENIED_REASON_GENERIC:
    return "MAV_ARM_AUTH_DENIED_REASON_GENERIC";
  case MAV_ARM_AUTH_DENIED_REASON_NONE:
    return "MAV_ARM_AUTH_DENIED_REASON_NONE";
  case MAV_ARM_AUTH_DENIED_REASON_INVALID_WAYPOINT:
    return "MAV_ARM_AUTH_DENIED_REASON_INVALID_WAYPOINT";
  case MAV_ARM_AUTH_DENIED_REASON_TIMEOUT:
    return "MAV_ARM_AUTH_DENIED_REASON_TIMEOUT";
  case MAV_ARM_AUTH_DENIED_REASON_AIRSPACE_IN_USE:
    return "MAV_ARM_AUTH_DENIED_REASON_AIRSPACE_IN_USE";
  case MAV_ARM_AUTH_DENIED_REASON_BAD_WEATHER:
    return "MAV_ARM_AUTH_DENIED_REASON_BAD_WEATHER";
  }
  return NULL;
};

static size_t
psnprint_MAV_ARM_AUTH_DENIED_REASON(char **p, size_t *sz,
                                    enum MAV_ARM_AUTH_DENIED_REASON val) {
  const char *s = MAV_ARM_AUTH_DENIED_REASON_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "MAV_ARM_AUTH_DENIED_REASON<%d>", (int)val);
}

static const char *RC_TYPE_str(enum RC_TYPE val) {
  switch (val) {
  case RC_TYPE_SPEKTRUM_DSM2:
    return "RC_TYPE_SPEKTRUM_DSM2";
  case RC_TYPE_SPEKTRUM_DSMX:
    return "RC_TYPE_SPEKTRUM_DSMX";
  }
  return NULL;
};

static size_t psnprint_RC_TYPE(char **p, size_t *sz, enum RC_TYPE val) {
  const char *s = RC_TYPE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "RC_TYPE<%d>", (int)val);
}

static const char *
POSITION_TARGET_TYPEMASK_str(enum POSITION_TARGET_TYPEMASK val) {
  switch (val) {
  case POSITION_TARGET_TYPEMASK_X_IGNORE:
    return "POSITION_TARGET_TYPEMASK_X_IGNORE";
  case POSITION_TARGET_TYPEMASK_Y_IGNORE:
    return "POSITION_TARGET_TYPEMASK_Y_IGNORE";
  case POSITION_TARGET_TYPEMASK_Z_IGNORE:
    return "POSITION_TARGET_TYPEMASK_Z_IGNORE";
  case POSITION_TARGET_TYPEMASK_VX_IGNORE:
    return "POSITION_TARGET_TYPEMASK_VX_IGNORE";
  case POSITION_TARGET_TYPEMASK_VY_IGNORE:
    return "POSITION_TARGET_TYPEMASK_VY_IGNORE";
  case POSITION_TARGET_TYPEMASK_VZ_IGNORE:
    return "POSITION_TARGET_TYPEMASK_VZ_IGNORE";
  case POSITION_TARGET_TYPEMASK_AX_IGNORE:
    return "POSITION_TARGET_TYPEMASK_AX_IGNORE";
  case POSITION_TARGET_TYPEMASK_AY_IGNORE:
    return "POSITION_TARGET_TYPEMASK_AY_IGNORE";
  case POSITION_TARGET_TYPEMASK_AZ_IGNORE:
    return "POSITION_TARGET_TYPEMASK_AZ_IGNORE";
  case POSITION_TARGET_TYPEMASK_FORCE_SET:
    return "POSITION_TARGET_TYPEMASK_FORCE_SET";
  case POSITION_TARGET_TYPEMASK_YAW_IGNORE:
    return "POSITION_TARGET_TYPEMASK_YAW_IGNORE";
  case POSITION_TARGET_TYPEMASK_YAW_RATE_IGNORE:
    return "POSITION_TARGET_TYPEMASK_YAW_RATE_IGNORE";
  }
  return NULL;
};

static size_t
psnprint_POSITION_TARGET_TYPEMASK(char **p, size_t *sz,
                                  enum POSITION_TARGET_TYPEMASK val) {
  const char *s = POSITION_TARGET_TYPEMASK_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "POSITION_TARGET_TYPEMASK<%d>", (int)val);
}

static const char *UTM_FLIGHT_STATE_str(enum UTM_FLIGHT_STATE val) {
  switch (val) {
  case UTM_FLIGHT_STATE_UNKNOWN:
    return "UTM_FLIGHT_STATE_UNKNOWN";
  case UTM_FLIGHT_STATE_GROUND:
    return "UTM_FLIGHT_STATE_GROUND";
  case UTM_FLIGHT_STATE_AIRBORNE:
    return "UTM_FLIGHT_STATE_AIRBORNE";
  case UTM_FLIGHT_STATE_EMERGENCY:
    return "UTM_FLIGHT_STATE_EMERGENCY";
  case UTM_FLIGHT_STATE_NOCTRL:
    return "UTM_FLIGHT_STATE_NOCTRL";
  }
  return NULL;
};

static size_t psnprint_UTM_FLIGHT_STATE(char **p, size_t *sz,
                                        enum UTM_FLIGHT_STATE val) {
  const char *s = UTM_FLIGHT_STATE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "UTM_FLIGHT_STATE<%d>", (int)val);
}

static const char *UTM_DATA_AVAIL_FLAGS_str(enum UTM_DATA_AVAIL_FLAGS val) {
  switch (val) {
  case UTM_DATA_AVAIL_FLAGS_TIME_VALID:
    return "UTM_DATA_AVAIL_FLAGS_TIME_VALID";
  case UTM_DATA_AVAIL_FLAGS_UAS_ID_AVAILABLE:
    return "UTM_DATA_AVAIL_FLAGS_UAS_ID_AVAILABLE";
  case UTM_DATA_AVAIL_FLAGS_POSITION_AVAILABLE:
    return "UTM_DATA_AVAIL_FLAGS_POSITION_AVAILABLE";
  case UTM_DATA_AVAIL_FLAGS_ALTITUDE_AVAILABLE:
    return "UTM_DATA_AVAIL_FLAGS_ALTITUDE_AVAILABLE";
  case UTM_DATA_AVAIL_FLAGS_RELATIVE_ALTITUDE_AVAILABLE:
    return "UTM_DATA_AVAIL_FLAGS_RELATIVE_ALTITUDE_AVAILABLE";
  case UTM_DATA_AVAIL_FLAGS_HORIZONTAL_VELO_AVAILABLE:
    return "UTM_DATA_AVAIL_FLAGS_HORIZONTAL_VELO_AVAILABLE";
  case UTM_DATA_AVAIL_FLAGS_VERTICAL_VELO_AVAILABLE:
    return "UTM_DATA_AVAIL_FLAGS_VERTICAL_VELO_AVAILABLE";
  case UTM_DATA_AVAIL_FLAGS_NEXT_WAYPOINT_AVAILABLE:
    return "UTM_DATA_AVAIL_FLAGS_NEXT_WAYPOINT_AVAILABLE";
  }
  return NULL;
};

static size_t psnprint_UTM_DATA_AVAIL_FLAGS(char **p, size_t *sz,
                                            enum UTM_DATA_AVAIL_FLAGS val) {
  const char *s = UTM_DATA_AVAIL_FLAGS_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "UTM_DATA_AVAIL_FLAGS<%d>", (int)val);
}

static const char *
CELLULAR_NETWORK_RADIO_TYPE_str(enum CELLULAR_NETWORK_RADIO_TYPE val) {
  switch (val) {
  case CELLULAR_NETWORK_RADIO_TYPE_NONE:
    return "CELLULAR_NETWORK_RADIO_TYPE_NONE";
  case CELLULAR_NETWORK_RADIO_TYPE_GSM:
    return "CELLULAR_NETWORK_RADIO_TYPE_GSM";
  case CELLULAR_NETWORK_RADIO_TYPE_CDMA:
    return "CELLULAR_NETWORK_RADIO_TYPE_CDMA";
  case CELLULAR_NETWORK_RADIO_TYPE_WCDMA:
    return "CELLULAR_NETWORK_RADIO_TYPE_WCDMA";
  case CELLULAR_NETWORK_RADIO_TYPE_LTE:
    return "CELLULAR_NETWORK_RADIO_TYPE_LTE";
  }
  return NULL;
};

static size_t
psnprint_CELLULAR_NETWORK_RADIO_TYPE(char **p, size_t *sz,
                                     enum CELLULAR_NETWORK_RADIO_TYPE val) {
  const char *s = CELLULAR_NETWORK_RADIO_TYPE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "CELLULAR_NETWORK_RADIO_TYPE<%d>", (int)val);
}

static const char *
CELLULAR_NETWORK_STATUS_FLAG_str(enum CELLULAR_NETWORK_STATUS_FLAG val) {
  switch (val) {
  case CELLULAR_NETWORK_STATUS_FLAG_ROAMING:
    return "CELLULAR_NETWORK_STATUS_FLAG_ROAMING";
  }
  return NULL;
};

static size_t
psnprint_CELLULAR_NETWORK_STATUS_FLAG(char **p, size_t *sz,
                                      enum CELLULAR_NETWORK_STATUS_FLAG val) {
  const char *s = CELLULAR_NETWORK_STATUS_FLAG_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "CELLULAR_NETWORK_STATUS_FLAG<%d>", (int)val);
}

static const char *PRECISION_LAND_MODE_str(enum PRECISION_LAND_MODE val) {
  switch (val) {
  case PRECISION_LAND_MODE_DISABLED:
    return "PRECISION_LAND_MODE_DISABLED";
  case PRECISION_LAND_MODE_OPPORTUNISTIC:
    return "PRECISION_LAND_MODE_OPPORTUNISTIC";
  case PRECISION_LAND_MODE_REQUIRED:
    return "PRECISION_LAND_MODE_REQUIRED";
  }
  return NULL;
};

static size_t psnprint_PRECISION_LAND_MODE(char **p, size_t *sz,
                                           enum PRECISION_LAND_MODE val) {
  const char *s = PRECISION_LAND_MODE_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "PRECISION_LAND_MODE<%d>", (int)val);
}

static const char *PARACHUTE_ACTION_str(enum PARACHUTE_ACTION val) {
  switch (val) {
  case PARACHUTE_DISABLE:
    return "PARACHUTE_DISABLE";
  case PARACHUTE_ENABLE:
    return "PARACHUTE_ENABLE";
  case PARACHUTE_RELEASE:
    return "PARACHUTE_RELEASE";
  }
  return NULL;
};

static size_t psnprint_PARACHUTE_ACTION(char **p, size_t *sz,
                                        enum PARACHUTE_ACTION val) {
  const char *s = PARACHUTE_ACTION_str(val);
  if (s) {
    return psnprintf(p, sz, "%s", s);
  }
  return psnprintf(p, sz, "PARACHUTE_ACTION<%d>", (int)val);
}

size_t common_message_snprintf(char *buf, size_t size,
                               const struct common_message *msg) {
  size_t r = 0;
  switch (msg->msg_id) {

  case COMMON_MESSAGE_HEARTBEAT:

    r += psnprintf(&buf, &size, "HEARTBEAT<");

    r += psnprintf(&buf, &size, " custom_mode:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " autopilot:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " base_mode:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " system_status:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " mavlink_version:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_SYS_STATUS:

    r += psnprintf(&buf, &size, "SYS_STATUS<");

    r += psnprintf(&buf, &size, " onboard_control_sensors_present:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " onboard_control_sensors_enabled:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " onboard_control_sensors_health:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " load:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " voltage_battery:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " current_battery:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " drop_rate_comm:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " errors_comm:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " errors_count1:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " errors_count2:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " errors_count3:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " errors_count4:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " battery_remaining:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_SYSTEM_TIME:

    r += psnprintf(&buf, &size, "SYSTEM_TIME<");

    r += psnprintf(&buf, &size, " time_unix_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_PING:

    r += psnprintf(&buf, &size, "PING<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " seq:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_CHANGE_OPERATOR_CONTROL:

    r += psnprintf(&buf, &size, "CHANGE_OPERATOR_CONTROL<");

    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " control_request:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " version:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " passkey:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_CHANGE_OPERATOR_CONTROL_ACK:

    r += psnprintf(&buf, &size, "CHANGE_OPERATOR_CONTROL_ACK<");

    r += psnprintf(&buf, &size, " gcs_system_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " control_request:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ack:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_AUTH_KEY:

    r += psnprintf(&buf, &size, "AUTH_KEY<");

    r += psnprintf(&buf, &size, " key:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_LINK_NODE_STATUS:

    r += psnprintf(&buf, &size, "LINK_NODE_STATUS<");

    r += psnprintf(&buf, &size, " timestamp:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " tx_rate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rx_rate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " messages_sent:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " messages_received:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " messages_lost:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rx_parse_err:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " tx_overflows:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rx_overflows:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " tx_buf:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rx_buf:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_SET_MODE:

    r += psnprintf(&buf, &size, "SET_MODE<");

    r += psnprintf(&buf, &size, " custom_mode:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " base_mode:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_PARAM_REQUEST_READ:

    r += psnprintf(&buf, &size, "PARAM_REQUEST_READ<");

    r += psnprintf(&buf, &size, " param_index:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_PARAM_REQUEST_LIST:

    r += psnprintf(&buf, &size, "PARAM_REQUEST_LIST<");

    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_PARAM_VALUE:

    r += psnprintf(&buf, &size, "PARAM_VALUE<");

    r += psnprintf(&buf, &size, " param_value:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param_count:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param_index:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_PARAM_SET:

    r += psnprintf(&buf, &size, "PARAM_SET<");

    r += psnprintf(&buf, &size, " param_value:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_GPS_RAW_INT:

    r += psnprintf(&buf, &size, "GPS_RAW_INT<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lat:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lon:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " alt:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " eph:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " epv:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vel:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " cog:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " fix_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " satellites_visible:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " alt_ellipsoid:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " h_acc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " v_acc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vel_acc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " hdg_acc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_GPS_STATUS:

    r += psnprintf(&buf, &size, "GPS_STATUS<");

    r += psnprintf(&buf, &size, " satellites_visible:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " satellite_prn:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " satellite_used:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " satellite_elevation:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " satellite_azimuth:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " satellite_snr:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_SCALED_IMU:

    r += psnprintf(&buf, &size, "SCALED_IMU<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " xacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " zacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " xgyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ygyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " zgyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " xmag:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ymag:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " zmag:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_RAW_IMU:

    r += psnprintf(&buf, &size, "RAW_IMU<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " xacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " zacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " xgyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ygyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " zgyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " xmag:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ymag:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " zmag:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_RAW_PRESSURE:

    r += psnprintf(&buf, &size, "RAW_PRESSURE<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " press_abs:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " press_diff1:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " press_diff2:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " temperature:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_SCALED_PRESSURE:

    r += psnprintf(&buf, &size, "SCALED_PRESSURE<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " press_abs:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " press_diff:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " temperature:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_ATTITUDE:

    r += psnprintf(&buf, &size, "ATTITUDE<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " roll:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pitch:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yaw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rollspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pitchspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yawspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_ATTITUDE_QUATERNION:

    r += psnprintf(&buf, &size, "ATTITUDE_QUATERNION<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " q1:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " q2:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " q3:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " q4:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rollspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pitchspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yawspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_LOCAL_POSITION_NED:

    r += psnprintf(&buf, &size, "LOCAL_POSITION_NED<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vx:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vz:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_GLOBAL_POSITION_INT:

    r += psnprintf(&buf, &size, "GLOBAL_POSITION_INT<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lat:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lon:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " alt:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " relative_alt:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vx:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vz:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " hdg:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_RC_CHANNELS_SCALED:

    r += psnprintf(&buf, &size, "RC_CHANNELS_SCALED<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan1_scaled:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan2_scaled:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan3_scaled:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan4_scaled:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan5_scaled:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan6_scaled:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan7_scaled:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan8_scaled:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " port:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rssi:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_RC_CHANNELS_RAW:

    r += psnprintf(&buf, &size, "RC_CHANNELS_RAW<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan1_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan2_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan3_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan4_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan5_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan6_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan7_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan8_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " port:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rssi:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_SERVO_OUTPUT_RAW:

    r += psnprintf(&buf, &size, "SERVO_OUTPUT_RAW<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " servo1_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " servo2_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " servo3_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " servo4_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " servo5_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " servo6_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " servo7_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " servo8_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " port:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " servo9_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " servo10_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " servo11_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " servo12_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " servo13_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " servo14_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " servo15_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " servo16_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_MISSION_REQUEST_PARTIAL_LIST:

    r += psnprintf(&buf, &size, "MISSION_REQUEST_PARTIAL_LIST<");

    r += psnprintf(&buf, &size, " start_index:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " end_index:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " mission_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_MISSION_WRITE_PARTIAL_LIST:

    r += psnprintf(&buf, &size, "MISSION_WRITE_PARTIAL_LIST<");

    r += psnprintf(&buf, &size, " start_index:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " end_index:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " mission_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_MISSION_ITEM:

    r += psnprintf(&buf, &size, "MISSION_ITEM<");

    r += psnprintf(&buf, &size, " param1:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param2:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param3:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param4:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " seq:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " command:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " frame:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " current:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " autocontinue:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " mission_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_MISSION_REQUEST:

    r += psnprintf(&buf, &size, "MISSION_REQUEST<");

    r += psnprintf(&buf, &size, " seq:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " mission_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_MISSION_SET_CURRENT:

    r += psnprintf(&buf, &size, "MISSION_SET_CURRENT<");

    r += psnprintf(&buf, &size, " seq:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_MISSION_CURRENT:

    r += psnprintf(&buf, &size, "MISSION_CURRENT<");

    r += psnprintf(&buf, &size, " seq:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_MISSION_REQUEST_LIST:

    r += psnprintf(&buf, &size, "MISSION_REQUEST_LIST<");

    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " mission_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_MISSION_COUNT:

    r += psnprintf(&buf, &size, "MISSION_COUNT<");

    r += psnprintf(&buf, &size, " count:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " mission_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_MISSION_CLEAR_ALL:

    r += psnprintf(&buf, &size, "MISSION_CLEAR_ALL<");

    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " mission_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_MISSION_ITEM_REACHED:

    r += psnprintf(&buf, &size, "MISSION_ITEM_REACHED<");

    r += psnprintf(&buf, &size, " seq:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_MISSION_ACK:

    r += psnprintf(&buf, &size, "MISSION_ACK<");

    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " mission_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_SET_GPS_GLOBAL_ORIGIN:

    r += psnprintf(&buf, &size, "SET_GPS_GLOBAL_ORIGIN<");

    r += psnprintf(&buf, &size, " latitude:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " longitude:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " altitude:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_GPS_GLOBAL_ORIGIN:

    r += psnprintf(&buf, &size, "GPS_GLOBAL_ORIGIN<");

    r += psnprintf(&buf, &size, " latitude:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " longitude:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " altitude:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_PARAM_MAP_RC:

    r += psnprintf(&buf, &size, "PARAM_MAP_RC<");

    r += psnprintf(&buf, &size, " param_value0:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " scale:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param_value_min:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param_value_max:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param_index:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " parameter_rc_channel_index:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_MISSION_REQUEST_INT:

    r += psnprintf(&buf, &size, "MISSION_REQUEST_INT<");

    r += psnprintf(&buf, &size, " seq:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " mission_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_MISSION_CHANGED:

    r += psnprintf(&buf, &size, "MISSION_CHANGED<");

    r += psnprintf(&buf, &size, " start_index:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " end_index:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " origin_sysid:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " origin_compid:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " mission_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_SAFETY_SET_ALLOWED_AREA:

    r += psnprintf(&buf, &size, "SAFETY_SET_ALLOWED_AREA<");

    r += psnprintf(&buf, &size, " p1x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " p1y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " p1z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " p2x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " p2y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " p2z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " frame:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_SAFETY_ALLOWED_AREA:

    r += psnprintf(&buf, &size, "SAFETY_ALLOWED_AREA<");

    r += psnprintf(&buf, &size, " p1x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " p1y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " p1z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " p2x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " p2y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " p2z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " frame:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_ATTITUDE_QUATERNION_COV:

    r += psnprintf(&buf, &size, "ATTITUDE_QUATERNION_COV<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " q:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rollspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pitchspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yawspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " covariance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_NAV_CONTROLLER_OUTPUT:

    r += psnprintf(&buf, &size, "NAV_CONTROLLER_OUTPUT<");

    r += psnprintf(&buf, &size, " nav_roll:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " nav_pitch:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " alt_error:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " aspd_error:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " xtrack_error:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " nav_bearing:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_bearing:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " wp_dist:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_GLOBAL_POSITION_INT_COV:

    r += psnprintf(&buf, &size, "GLOBAL_POSITION_INT_COV<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lat:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lon:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " alt:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " relative_alt:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vx:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vz:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " covariance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " estimator_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_LOCAL_POSITION_NED_COV:

    r += psnprintf(&buf, &size, "LOCAL_POSITION_NED_COV<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vx:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vz:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ax:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ay:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " az:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " covariance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " estimator_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_RC_CHANNELS:

    r += psnprintf(&buf, &size, "RC_CHANNELS<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan1_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan2_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan3_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan4_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan5_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan6_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan7_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan8_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan9_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan10_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan11_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan12_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan13_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan14_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan15_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan16_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan17_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan18_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chancount:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rssi:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_REQUEST_DATA_STREAM:

    r += psnprintf(&buf, &size, "REQUEST_DATA_STREAM<");

    r += psnprintf(&buf, &size, " req_message_rate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " req_stream_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " start_stop:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_DATA_STREAM:

    r += psnprintf(&buf, &size, "DATA_STREAM<");

    r += psnprintf(&buf, &size, " message_rate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " stream_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " on_off:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_MANUAL_CONTROL:

    r += psnprintf(&buf, &size, "MANUAL_CONTROL<");

    r += psnprintf(&buf, &size, " x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " r:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " buttons:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_RC_CHANNELS_OVERRIDE:

    r += psnprintf(&buf, &size, "RC_CHANNELS_OVERRIDE<");

    r += psnprintf(&buf, &size, " chan1_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan2_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan3_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan4_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan5_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan6_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan7_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan8_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan9_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan10_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan11_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan12_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan13_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan14_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan15_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan16_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan17_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan18_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_MISSION_ITEM_INT:

    r += psnprintf(&buf, &size, "MISSION_ITEM_INT<");

    r += psnprintf(&buf, &size, " param1:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param2:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param3:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param4:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " seq:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " command:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " frame:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " current:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " autocontinue:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " mission_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_VFR_HUD:

    r += psnprintf(&buf, &size, "VFR_HUD<");

    r += psnprintf(&buf, &size, " airspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " groundspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " alt:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " climb:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " heading:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " throttle:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_COMMAND_INT:

    r += psnprintf(&buf, &size, "COMMAND_INT<");

    r += psnprintf(&buf, &size, " param1:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param2:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param3:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param4:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " command:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " frame:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " current:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " autocontinue:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_COMMAND_LONG:

    r += psnprintf(&buf, &size, "COMMAND_LONG<");

    r += psnprintf(&buf, &size, " param1:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param2:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param3:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param4:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param5:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param6:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param7:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " command:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " confirmation:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_COMMAND_ACK:

    r += psnprintf(&buf, &size, "COMMAND_ACK<");

    r += psnprintf(&buf, &size, " command:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " result:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " result_param2:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " progress:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_MANUAL_SETPOINT:

    r += psnprintf(&buf, &size, "MANUAL_SETPOINT<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " roll:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pitch:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yaw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " thrust:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " mode_switch:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " manual_override_switch:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_SET_ATTITUDE_TARGET:

    r += psnprintf(&buf, &size, "SET_ATTITUDE_TARGET<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " q:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " body_roll_rate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " body_pitch_rate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " body_yaw_rate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " thrust:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " type_mask:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_ATTITUDE_TARGET:

    r += psnprintf(&buf, &size, "ATTITUDE_TARGET<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " q:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " body_roll_rate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " body_pitch_rate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " body_yaw_rate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " thrust:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " type_mask:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_SET_POSITION_TARGET_LOCAL_NED:

    r += psnprintf(&buf, &size, "SET_POSITION_TARGET_LOCAL_NED<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vx:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vz:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " afx:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " afy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " afz:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yaw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yaw_rate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " type_mask:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " coordinate_frame:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_POSITION_TARGET_LOCAL_NED:

    r += psnprintf(&buf, &size, "POSITION_TARGET_LOCAL_NED<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vx:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vz:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " afx:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " afy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " afz:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yaw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yaw_rate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " type_mask:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " coordinate_frame:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_SET_POSITION_TARGET_GLOBAL_INT:

    r += psnprintf(&buf, &size, "SET_POSITION_TARGET_GLOBAL_INT<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lat_int:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lon_int:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " alt:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vx:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vz:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " afx:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " afy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " afz:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yaw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yaw_rate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " type_mask:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " coordinate_frame:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_POSITION_TARGET_GLOBAL_INT:

    r += psnprintf(&buf, &size, "POSITION_TARGET_GLOBAL_INT<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lat_int:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lon_int:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " alt:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vx:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vz:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " afx:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " afy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " afz:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yaw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yaw_rate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " type_mask:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " coordinate_frame:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_LOCAL_POSITION_NED_SYSTEM_GLOBAL_OFFSET:

    r += psnprintf(&buf, &size, "LOCAL_POSITION_NED_SYSTEM_GLOBAL_OFFSET<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " roll:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pitch:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yaw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_HIL_STATE:

    r += psnprintf(&buf, &size, "HIL_STATE<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " roll:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pitch:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yaw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rollspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pitchspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yawspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lat:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lon:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " alt:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vx:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vz:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " xacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " zacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_HIL_CONTROLS:

    r += psnprintf(&buf, &size, "HIL_CONTROLS<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " roll_ailerons:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pitch_elevator:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yaw_rudder:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " throttle:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " aux1:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " aux2:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " aux3:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " aux4:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " mode:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " nav_mode:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_HIL_RC_INPUTS_RAW:

    r += psnprintf(&buf, &size, "HIL_RC_INPUTS_RAW<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan1_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan2_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan3_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan4_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan5_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan6_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan7_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan8_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan9_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan10_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan11_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " chan12_raw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rssi:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_HIL_ACTUATOR_CONTROLS:

    r += psnprintf(&buf, &size, "HIL_ACTUATOR_CONTROLS<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " flags:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " controls:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " mode:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_OPTICAL_FLOW:

    r += psnprintf(&buf, &size, "OPTICAL_FLOW<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " flow_comp_m_x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " flow_comp_m_y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ground_distance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " flow_x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " flow_y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " sensor_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " quality:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " flow_rate_x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " flow_rate_y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_GLOBAL_VISION_POSITION_ESTIMATE:

    r += psnprintf(&buf, &size, "GLOBAL_VISION_POSITION_ESTIMATE<");

    r += psnprintf(&buf, &size, " usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " roll:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pitch:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yaw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " covariance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " reset_counter:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_VISION_POSITION_ESTIMATE:

    r += psnprintf(&buf, &size, "VISION_POSITION_ESTIMATE<");

    r += psnprintf(&buf, &size, " usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " roll:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pitch:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yaw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " covariance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " reset_counter:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_VISION_SPEED_ESTIMATE:

    r += psnprintf(&buf, &size, "VISION_SPEED_ESTIMATE<");

    r += psnprintf(&buf, &size, " usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " covariance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " reset_counter:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_VICON_POSITION_ESTIMATE:

    r += psnprintf(&buf, &size, "VICON_POSITION_ESTIMATE<");

    r += psnprintf(&buf, &size, " usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " roll:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pitch:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yaw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " covariance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_HIGHRES_IMU:

    r += psnprintf(&buf, &size, "HIGHRES_IMU<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " xacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " zacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " xgyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ygyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " zgyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " xmag:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ymag:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " zmag:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " abs_pressure:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " diff_pressure:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pressure_alt:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " temperature:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " fields_updated:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_OPTICAL_FLOW_RAD:

    r += psnprintf(&buf, &size, "OPTICAL_FLOW_RAD<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " integration_time_us:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " integrated_x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " integrated_y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " integrated_xgyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " integrated_ygyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " integrated_zgyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " time_delta_distance_us:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " distance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " temperature:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " sensor_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " quality:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_HIL_SENSOR:

    r += psnprintf(&buf, &size, "HIL_SENSOR<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " xacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " zacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " xgyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ygyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " zgyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " xmag:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ymag:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " zmag:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " abs_pressure:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " diff_pressure:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pressure_alt:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " temperature:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " fields_updated:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_SIM_STATE:

    r += psnprintf(&buf, &size, "SIM_STATE<");

    r += psnprintf(&buf, &size, " q1:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " q2:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " q3:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " q4:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " roll:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pitch:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yaw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " xacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " zacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " xgyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ygyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " zgyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lat:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lon:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " alt:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " std_dev_horz:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " std_dev_vert:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vn:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ve:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vd:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_RADIO_STATUS:

    r += psnprintf(&buf, &size, "RADIO_STATUS<");

    r += psnprintf(&buf, &size, " rxerrors:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " fixed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rssi:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " remrssi:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " txbuf:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " noise:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " remnoise:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_FILE_TRANSFER_PROTOCOL:

    r += psnprintf(&buf, &size, "FILE_TRANSFER_PROTOCOL<");

    r += psnprintf(&buf, &size, " target_network:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " payload:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_TIMESYNC:

    r += psnprintf(&buf, &size, "TIMESYNC<");

    r += psnprintf(&buf, &size, " tc1:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ts1:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_CAMERA_TRIGGER:

    r += psnprintf(&buf, &size, "CAMERA_TRIGGER<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " seq:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_HIL_GPS:

    r += psnprintf(&buf, &size, "HIL_GPS<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lat:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lon:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " alt:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " eph:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " epv:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vel:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vn:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ve:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vd:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " cog:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " fix_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " satellites_visible:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_HIL_OPTICAL_FLOW:

    r += psnprintf(&buf, &size, "HIL_OPTICAL_FLOW<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " integration_time_us:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " integrated_x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " integrated_y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " integrated_xgyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " integrated_ygyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " integrated_zgyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " time_delta_distance_us:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " distance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " temperature:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " sensor_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " quality:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_HIL_STATE_QUATERNION:

    r += psnprintf(&buf, &size, "HIL_STATE_QUATERNION<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " attitude_quaternion:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rollspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pitchspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yawspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lat:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lon:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " alt:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vx:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vz:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ind_airspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " true_airspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " xacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " zacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_SCALED_IMU2:

    r += psnprintf(&buf, &size, "SCALED_IMU2<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " xacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " zacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " xgyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ygyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " zgyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " xmag:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ymag:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " zmag:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_LOG_REQUEST_LIST:

    r += psnprintf(&buf, &size, "LOG_REQUEST_LIST<");

    r += psnprintf(&buf, &size, " start:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " end:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_LOG_ENTRY:

    r += psnprintf(&buf, &size, "LOG_ENTRY<");

    r += psnprintf(&buf, &size, " time_utc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " size:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " num_logs:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " last_log_num:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_LOG_REQUEST_DATA:

    r += psnprintf(&buf, &size, "LOG_REQUEST_DATA<");

    r += psnprintf(&buf, &size, " ofs:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " count:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_LOG_DATA:

    r += psnprintf(&buf, &size, "LOG_DATA<");

    r += psnprintf(&buf, &size, " ofs:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " count:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " data:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_LOG_ERASE:

    r += psnprintf(&buf, &size, "LOG_ERASE<");

    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_LOG_REQUEST_END:

    r += psnprintf(&buf, &size, "LOG_REQUEST_END<");

    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_GPS_INJECT_DATA:

    r += psnprintf(&buf, &size, "GPS_INJECT_DATA<");

    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " len:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " data:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_GPS2_RAW:

    r += psnprintf(&buf, &size, "GPS2_RAW<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lat:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lon:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " alt:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " dgps_age:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " eph:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " epv:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vel:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " cog:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " fix_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " satellites_visible:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " dgps_numch:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_POWER_STATUS:

    r += psnprintf(&buf, &size, "POWER_STATUS<");

    r += psnprintf(&buf, &size, " Vcc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " Vservo:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " flags:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_SERIAL_CONTROL:

    r += psnprintf(&buf, &size, "SERIAL_CONTROL<");

    r += psnprintf(&buf, &size, " baudrate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " timeout:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " device:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " flags:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " count:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " data:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_GPS_RTK:

    r += psnprintf(&buf, &size, "GPS_RTK<");

    r += psnprintf(&buf, &size, " time_last_baseline_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " tow:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " baseline_a_mm:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " baseline_b_mm:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " baseline_c_mm:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " accuracy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " iar_num_hypotheses:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " wn:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rtk_receiver_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rtk_health:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rtk_rate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " nsats:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " baseline_coords_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_GPS2_RTK:

    r += psnprintf(&buf, &size, "GPS2_RTK<");

    r += psnprintf(&buf, &size, " time_last_baseline_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " tow:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " baseline_a_mm:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " baseline_b_mm:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " baseline_c_mm:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " accuracy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " iar_num_hypotheses:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " wn:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rtk_receiver_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rtk_health:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rtk_rate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " nsats:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " baseline_coords_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_SCALED_IMU3:

    r += psnprintf(&buf, &size, "SCALED_IMU3<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " xacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " zacc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " xgyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ygyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " zgyro:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " xmag:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ymag:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " zmag:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_DATA_TRANSMISSION_HANDSHAKE:

    r += psnprintf(&buf, &size, "DATA_TRANSMISSION_HANDSHAKE<");

    r += psnprintf(&buf, &size, " size:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " width:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " height:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " packets:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " payload:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " jpg_quality:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_ENCAPSULATED_DATA:

    r += psnprintf(&buf, &size, "ENCAPSULATED_DATA<");

    r += psnprintf(&buf, &size, " seqnr:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " data:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_DISTANCE_SENSOR:

    r += psnprintf(&buf, &size, "DISTANCE_SENSOR<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " min_distance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " max_distance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " current_distance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " orientation:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " covariance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " horizontal_fov:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vertical_fov:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " quaternion:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_TERRAIN_REQUEST:

    r += psnprintf(&buf, &size, "TERRAIN_REQUEST<");

    r += psnprintf(&buf, &size, " mask:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lat:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lon:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " grid_spacing:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_TERRAIN_DATA:

    r += psnprintf(&buf, &size, "TERRAIN_DATA<");

    r += psnprintf(&buf, &size, " lat:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lon:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " grid_spacing:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " data:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " gridbit:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_TERRAIN_CHECK:

    r += psnprintf(&buf, &size, "TERRAIN_CHECK<");

    r += psnprintf(&buf, &size, " lat:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lon:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_TERRAIN_REPORT:

    r += psnprintf(&buf, &size, "TERRAIN_REPORT<");

    r += psnprintf(&buf, &size, " lat:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lon:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " terrain_height:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " current_height:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " spacing:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pending:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " loaded:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_SCALED_PRESSURE2:

    r += psnprintf(&buf, &size, "SCALED_PRESSURE2<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " press_abs:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " press_diff:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " temperature:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_ATT_POS_MOCAP:

    r += psnprintf(&buf, &size, "ATT_POS_MOCAP<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " q:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " covariance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_SET_ACTUATOR_CONTROL_TARGET:

    r += psnprintf(&buf, &size, "SET_ACTUATOR_CONTROL_TARGET<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " controls:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " group_mlx:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_ACTUATOR_CONTROL_TARGET:

    r += psnprintf(&buf, &size, "ACTUATOR_CONTROL_TARGET<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " controls:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " group_mlx:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_ALTITUDE:

    r += psnprintf(&buf, &size, "ALTITUDE<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " altitude_monotonic:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " altitude_amsl:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " altitude_local:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " altitude_relative:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " altitude_terrain:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " bottom_clearance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_RESOURCE_REQUEST:

    r += psnprintf(&buf, &size, "RESOURCE_REQUEST<");

    r += psnprintf(&buf, &size, " request_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " uri_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " uri:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " transfer_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " storage:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_SCALED_PRESSURE3:

    r += psnprintf(&buf, &size, "SCALED_PRESSURE3<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " press_abs:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " press_diff:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " temperature:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_FOLLOW_TARGET:

    r += psnprintf(&buf, &size, "FOLLOW_TARGET<");

    r += psnprintf(&buf, &size, " timestamp:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " custom_state:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lat:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lon:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " alt:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vel:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " acc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " attitude_q:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rates:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " position_cov:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " est_capabilities:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_CONTROL_SYSTEM_STATE:

    r += psnprintf(&buf, &size, "CONTROL_SYSTEM_STATE<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " x_acc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " y_acc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " z_acc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " x_vel:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " y_vel:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " z_vel:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " x_pos:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " y_pos:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " z_pos:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " airspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vel_variance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pos_variance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " q:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " roll_rate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pitch_rate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yaw_rate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_BATTERY_STATUS:

    r += psnprintf(&buf, &size, "BATTERY_STATUS<");

    r += psnprintf(&buf, &size, " current_consumed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " energy_consumed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " temperature:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " voltages:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " current_battery:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " battery_function:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " battery_remaining:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " time_remaining:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " charge_state:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_AUTOPILOT_VERSION:

    r += psnprintf(&buf, &size, "AUTOPILOT_VERSION<");

    r += psnprintf(&buf, &size, " capabilities:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " uid:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " flight_sw_version:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " middleware_sw_version:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " os_sw_version:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " board_version:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vendor_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " product_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " flight_custom_version:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " middleware_custom_version:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " os_custom_version:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " uid2:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_LANDING_TARGET:

    r += psnprintf(&buf, &size, "LANDING_TARGET<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " angle_x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " angle_y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " distance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " size_x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " size_y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_num:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " frame:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " q:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " position_valid:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_ESTIMATOR_STATUS:

    r += psnprintf(&buf, &size, "ESTIMATOR_STATUS<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vel_ratio:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pos_horiz_ratio:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pos_vert_ratio:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " mag_ratio:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " hagl_ratio:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " tas_ratio:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pos_horiz_accuracy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pos_vert_accuracy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " flags:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_WIND_COV:

    r += psnprintf(&buf, &size, "WIND_COV<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " wind_x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " wind_y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " wind_z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " var_horiz:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " var_vert:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " wind_alt:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " horiz_accuracy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vert_accuracy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_GPS_INPUT:

    r += psnprintf(&buf, &size, "GPS_INPUT<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " time_week_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lat:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lon:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " alt:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " hdop:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vdop:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vn:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ve:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vd:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " speed_accuracy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " horiz_accuracy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vert_accuracy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ignore_flags:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " time_week:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " gps_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " fix_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " satellites_visible:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_GPS_RTCM_DATA:

    r += psnprintf(&buf, &size, "GPS_RTCM_DATA<");

    r += psnprintf(&buf, &size, " flags:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " len:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " data:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_HIGH_LATENCY:

    r += psnprintf(&buf, &size, "HIGH_LATENCY<");

    r += psnprintf(&buf, &size, " custom_mode:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " latitude:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " longitude:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " roll:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pitch:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " heading:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " heading_sp:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " altitude_amsl:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " altitude_sp:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " wp_distance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " base_mode:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " landed_state:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " throttle:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " airspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " airspeed_sp:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " groundspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " climb_rate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " gps_nsat:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " gps_fix_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " battery_remaining:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " temperature:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " temperature_air:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " failsafe:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " wp_num:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_HIGH_LATENCY2:

    r += psnprintf(&buf, &size, "HIGH_LATENCY2<");

    r += psnprintf(&buf, &size, " timestamp:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " latitude:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " longitude:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " custom_mode:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " altitude:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_altitude:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_distance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " wp_num:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " failure_flags:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " autopilot:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " heading:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_heading:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " throttle:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " airspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " airspeed_sp:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " groundspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " windspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " wind_heading:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " eph:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " epv:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " temperature_air:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " climb_rate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " battery:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " custom0:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " custom1:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " custom2:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_VIBRATION:

    r += psnprintf(&buf, &size, "VIBRATION<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vibration_x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vibration_y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vibration_z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " clipping_0:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " clipping_1:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " clipping_2:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_HOME_POSITION:

    r += psnprintf(&buf, &size, "HOME_POSITION<");

    r += psnprintf(&buf, &size, " latitude:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " longitude:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " altitude:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " q:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " approach_x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " approach_y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " approach_z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_SET_HOME_POSITION:

    r += psnprintf(&buf, &size, "SET_HOME_POSITION<");

    r += psnprintf(&buf, &size, " latitude:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " longitude:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " altitude:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " q:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " approach_x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " approach_y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " approach_z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_MESSAGE_INTERVAL:

    r += psnprintf(&buf, &size, "MESSAGE_INTERVAL<");

    r += psnprintf(&buf, &size, " interval_us:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " message_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_EXTENDED_SYS_STATE:

    r += psnprintf(&buf, &size, "EXTENDED_SYS_STATE<");

    r += psnprintf(&buf, &size, " vtol_state:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " landed_state:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_ADSB_VEHICLE:

    r += psnprintf(&buf, &size, "ADSB_VEHICLE<");

    r += psnprintf(&buf, &size, " ICAO_address:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lat:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lon:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " altitude:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " heading:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " hor_velocity:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ver_velocity:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " flags:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " squawk:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " altitude_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " callsign:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " emitter_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " tslc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_COLLISION:

    r += psnprintf(&buf, &size, "COLLISION<");

    r += psnprintf(&buf, &size, " id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " time_to_minimum_delta:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " altitude_minimum_delta:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " horizontal_minimum_delta:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " src:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " action:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " threat_level:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_V2_EXTENSION:

    r += psnprintf(&buf, &size, "V2_EXTENSION<");

    r += psnprintf(&buf, &size, " message_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_network:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " payload:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_MEMORY_VECT:

    r += psnprintf(&buf, &size, "MEMORY_VECT<");

    r += psnprintf(&buf, &size, " address:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ver:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " value:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_DEBUG_VECT:

    r += psnprintf(&buf, &size, "DEBUG_VECT<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " name:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_NAMED_VALUE_FLOAT:

    r += psnprintf(&buf, &size, "NAMED_VALUE_FLOAT<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " value:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " name:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_NAMED_VALUE_INT:

    r += psnprintf(&buf, &size, "NAMED_VALUE_INT<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " value:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " name:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_STATUSTEXT:

    r += psnprintf(&buf, &size, "STATUSTEXT<");

    r += psnprintf(&buf, &size, " severity:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " text:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_DEBUG:

    r += psnprintf(&buf, &size, "DEBUG<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " value:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ind:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_SETUP_SIGNING:

    r += psnprintf(&buf, &size, "SETUP_SIGNING<");

    r += psnprintf(&buf, &size, " initial_timestamp:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " secret_key:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_BUTTON_CHANGE:

    r += psnprintf(&buf, &size, "BUTTON_CHANGE<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " last_change_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " state:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_PLAY_TUNE:

    r += psnprintf(&buf, &size, "PLAY_TUNE<");

    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " tune:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " tune2:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_CAMERA_INFORMATION:

    r += psnprintf(&buf, &size, "CAMERA_INFORMATION<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " firmware_version:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " focal_length:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " sensor_size_h:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " sensor_size_v:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " flags:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " resolution_h:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " resolution_v:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " cam_definition_version:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vendor_name:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " model_name:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lens_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " cam_definition_uri:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_CAMERA_SETTINGS:

    r += psnprintf(&buf, &size, "CAMERA_SETTINGS<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " mode_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " zoomLevel:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " focusLevel:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_STORAGE_INFORMATION:

    r += psnprintf(&buf, &size, "STORAGE_INFORMATION<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " total_capacity:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " used_capacity:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " available_capacity:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " read_speed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " write_speed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " storage_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " storage_count:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " status:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_CAMERA_CAPTURE_STATUS:

    r += psnprintf(&buf, &size, "CAMERA_CAPTURE_STATUS<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " image_interval:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " recording_time_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " available_capacity:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " image_status:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " video_status:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_CAMERA_IMAGE_CAPTURED:

    r += psnprintf(&buf, &size, "CAMERA_IMAGE_CAPTURED<");

    r += psnprintf(&buf, &size, " time_utc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lat:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lon:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " alt:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " relative_alt:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " q:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " image_index:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " camera_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " capture_result:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " file_url:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_FLIGHT_INFORMATION:

    r += psnprintf(&buf, &size, "FLIGHT_INFORMATION<");

    r += psnprintf(&buf, &size, " arming_time_utc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " takeoff_time_utc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " flight_uuid:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_MOUNT_ORIENTATION:

    r += psnprintf(&buf, &size, "MOUNT_ORIENTATION<");

    r += psnprintf(&buf, &size, " time_boot_ms:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " roll:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pitch:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yaw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yaw_absolute:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_LOGGING_DATA:

    r += psnprintf(&buf, &size, "LOGGING_DATA<");

    r += psnprintf(&buf, &size, " sequence:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " length:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " first_message_offset:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " data:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_LOGGING_DATA_ACKED:

    r += psnprintf(&buf, &size, "LOGGING_DATA_ACKED<");

    r += psnprintf(&buf, &size, " sequence:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " length:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " first_message_offset:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " data:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_LOGGING_ACK:

    r += psnprintf(&buf, &size, "LOGGING_ACK<");

    r += psnprintf(&buf, &size, " sequence:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_VIDEO_STREAM_INFORMATION:

    r += psnprintf(&buf, &size, "VIDEO_STREAM_INFORMATION<");

    r += psnprintf(&buf, &size, " framerate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " bitrate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " flags:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " resolution_h:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " resolution_v:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rotation:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " hfov:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " stream_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " count:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " name:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " uri:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_VIDEO_STREAM_STATUS:

    r += psnprintf(&buf, &size, "VIDEO_STREAM_STATUS<");

    r += psnprintf(&buf, &size, " framerate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " bitrate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " flags:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " resolution_h:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " resolution_v:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rotation:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " hfov:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " stream_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_WIFI_CONFIG_AP:

    r += psnprintf(&buf, &size, "WIFI_CONFIG_AP<");

    r += psnprintf(&buf, &size, " ssid:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " password:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_PROTOCOL_VERSION:

    r += psnprintf(&buf, &size, "PROTOCOL_VERSION<");

    r += psnprintf(&buf, &size, " version:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " min_version:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " max_version:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " spec_version_hash:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " library_version_hash:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_UAVCAN_NODE_STATUS:

    r += psnprintf(&buf, &size, "UAVCAN_NODE_STATUS<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " uptime_sec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vendor_specific_status_code:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " health:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " mode:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " sub_mode:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_UAVCAN_NODE_INFO:

    r += psnprintf(&buf, &size, "UAVCAN_NODE_INFO<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " uptime_sec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " sw_vcs_commit:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " name:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " hw_version_major:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " hw_version_minor:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " hw_unique_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " sw_version_major:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " sw_version_minor:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_PARAM_EXT_REQUEST_READ:

    r += psnprintf(&buf, &size, "PARAM_EXT_REQUEST_READ<");

    r += psnprintf(&buf, &size, " param_index:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_PARAM_EXT_REQUEST_LIST:

    r += psnprintf(&buf, &size, "PARAM_EXT_REQUEST_LIST<");

    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_PARAM_EXT_VALUE:

    r += psnprintf(&buf, &size, "PARAM_EXT_VALUE<");

    r += psnprintf(&buf, &size, " param_count:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param_index:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param_value:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_PARAM_EXT_SET:

    r += psnprintf(&buf, &size, "PARAM_EXT_SET<");

    r += psnprintf(&buf, &size, " target_system:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " target_component:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param_value:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_PARAM_EXT_ACK:

    r += psnprintf(&buf, &size, "PARAM_EXT_ACK<");

    r += psnprintf(&buf, &size, " param_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param_value:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " param_result:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_OBSTACLE_DISTANCE:

    r += psnprintf(&buf, &size, "OBSTACLE_DISTANCE<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " distances:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " min_distance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " max_distance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " sensor_type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " increment:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " increment_f:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " angle_offset:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_ODOMETRY:

    r += psnprintf(&buf, &size, "ODOMETRY<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " q:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vx:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vz:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rollspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pitchspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " yawspeed:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pose_covariance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " velocity_covariance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " frame_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " child_frame_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " reset_counter:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_TRAJECTORY_REPRESENTATION_WAYPOINTS:

    r += psnprintf(&buf, &size, "TRAJECTORY_REPRESENTATION_WAYPOINTS<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pos_x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pos_y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pos_z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vel_x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vel_y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vel_z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " acc_x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " acc_y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " acc_z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pos_yaw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vel_yaw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " valid_points:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_TRAJECTORY_REPRESENTATION_BEZIER:

    r += psnprintf(&buf, &size, "TRAJECTORY_REPRESENTATION_BEZIER<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pos_x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pos_y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pos_z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " delta:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " pos_yaw:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " valid_points:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_CELLULAR_STATUS:

    r += psnprintf(&buf, &size, "CELLULAR_STATUS<");

    r += psnprintf(&buf, &size, " cid:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " status:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " mcc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " mnc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lac:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " type:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " quality:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_ISBD_LINK_STATUS:

    r += psnprintf(&buf, &size, "ISBD_LINK_STATUS<");

    r += psnprintf(&buf, &size, " timestamp:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " last_heartbeat:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " failed_sessions:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " successful_sessions:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " signal_quality:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " ring_pending:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " tx_session_pending:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " rx_session_pending:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_UTM_GLOBAL_POSITION:

    r += psnprintf(&buf, &size, "UTM_GLOBAL_POSITION<");

    r += psnprintf(&buf, &size, " time:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lat:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " lon:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " alt:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " relative_alt:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " next_lat:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " next_lon:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " next_alt:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vx:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vy:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vz:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " h_acc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " v_acc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " vel_acc:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " update_rate:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " uas_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " flight_state:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " flags:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_DEBUG_FLOAT_ARRAY:

    r += psnprintf(&buf, &size, "DEBUG_FLOAT_ARRAY<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " array_id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " name:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " data:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_ORBIT_EXECUTION_STATUS:

    r += psnprintf(&buf, &size, "ORBIT_EXECUTION_STATUS<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " radius:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " x:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " y:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " z:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " frame:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_STATUSTEXT_LONG:

    r += psnprintf(&buf, &size, "STATUSTEXT_LONG<");

    r += psnprintf(&buf, &size, " severity:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " text:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_SMART_BATTERY_INFO:

    r += psnprintf(&buf, &size, "SMART_BATTERY_INFO<");

    r += psnprintf(&buf, &size, " capacity_full_specification:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " capacity_full:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " serial_number:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " cycle_count:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " weight:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " discharge_minimum_voltage:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " charging_minimum_voltage:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " resting_minimum_voltage:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " device_name:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_SMART_BATTERY_STATUS:

    r += psnprintf(&buf, &size, "SMART_BATTERY_STATUS<");

    r += psnprintf(&buf, &size, " fault_bitmask:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " time_remaining:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " id:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " capacity_remaining:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " current:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " temperature:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " cell_offset:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " voltages:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_ACTUATOR_OUTPUT_STATUS:

    r += psnprintf(&buf, &size, "ACTUATOR_OUTPUT_STATUS<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " active:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " actuator:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_TIME_ESTIMATE_TO_TARGET:

    r += psnprintf(&buf, &size, "TIME_ESTIMATE_TO_TARGET<");

    r += psnprintf(&buf, &size, " safe_return:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " land:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " mission_next_item:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " mission_end:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " commanded_action:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;

  case COMMON_MESSAGE_WHEEL_DISTANCE:

    r += psnprintf(&buf, &size, "WHEEL_DISTANCE<");

    r += psnprintf(&buf, &size, " time_usec:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " distance:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " count:");
    // TODO values: handle strings, arrays and enums
    r += psnprintf(&buf, &size, " >");
    break;
  };

  return r;
}
